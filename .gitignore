node_modules/
dist/

# Playwright
playwright/.auth/
playwright-report/
test-results/
blob-report/

# Downloaded reports / runtime artifacts
tmp/
logs/
output/
*.log

# Secrets
.env
.env.*
!.env.example
secrets/

# MCP config holds a real Supabase access token directly (${VAR} expansion
# confirmed broken in this IDE-embedded host, 2026-09-01 — see mcp-server-supabase
# setup notes) — never commit this file.
.mcp.json

# Local Claude Code config (personal, not project-scoped)
.claude/
