# คำสั่งเริ่มงาน — Paste ไฟล์นี้ทั้งไฟล์เป็น prompt แรกให้ Cursor ในโปรเจกต์ BigSeller-Inventory

คุณกำลังต่อยอด repo `BigSeller-Inventory` ที่มีอยู่แล้ว (Playwright + Google Sheets automation
สำหรับสร้างใบย้ายสินค้า) ให้กลายเป็นศูนย์กลางข้อมูล + ระบบสั่งงาน + เว็บแดชบอร์ด งานทั้งหมดมี spec
ละเอียดอยู่ในไฟล์ markdown ชุดหนึ่งแล้ว คุณไม่ต้องออกแบบเอง แค่ทำตามลำดับข้างล่างนี้

## ขั้นตอนที่ 1 — อ่านก่อนแตะโค้ดอะไรทั้งสิ้น
เปิดอ่านไฟล์เหล่านี้ตามลำดับ:
1. `SYSTEM-ARCHITECTURE.md` — ภาพรวมทั้งหมด, กับดักที่เคยเจอมาแล้ว (หัวข้อ 5), checklist สิ่งที่ยัง
   ไม่มีคำตอบ (หัวข้อ 7)
2. โค้ดจริงในโฟลเดอร์ `src/` และ `scripts/` ที่มีอยู่แล้ว — โดยเฉพาะ `src/utils/browser-runner.ts`,
   `src/bigseller/auth.ts`, `src/services/transfer-plan-service.ts`, `src/sheets/sheets-client.ts` —
   เพื่อเข้าใจ convention ที่ต้องทำตาม (ไม่ใช่แค่จำ pattern จาก spec เฉยๆ)

## ขั้นตอนที่ 2 — Build ตามลำดับนี้เท่านั้น (ห้ามสลับ ห้ามข้าม)

| ลำดับ | ไฟล์ spec | ทำคู่ขนานกับอันไหนได้ | เงื่อนไขก่อนเริ่ม |
|---|---|---|---|
| 1 | `FEATURE-pending-demand-and-offline-lock.md` | คู่ขนานกับ 2, 3, 4 ได้ | ไม่มี |
| 2 | `FEATURE-operator-picking-performance.md` | คู่ขนานกับ 1, 3, 4 ได้ | รอ spreadsheet ID/โครงสร้างชีตเป้าจากผู้ใช้ก่อนเขียนส่วนเทียบเป้า — ถ้ายังไม่ได้ ทำแค่ส่วน sync ตัวเลขจริงไปก่อน |
| 3 | `FEATURE-return-exception-audit.md` | คู่ขนานกับ 1, 2, 4 ได้ | ไม่มี |
| 4 | `FEATURE-sales-online-offline-report.md` | คู่ขนานกับ 1, 2, 3 ได้ | **ต้องทำการทดสอบ day-cutoff (หัวข้อ 1b ในไฟล์นั้น) ก่อน ห้ามตั้ง schedule จริงจนกว่าจะทดสอบเสร็จ** |
| 5 | `FEATURE-web-dashboard-supabase.md` | ต้องรอ 1/2/3/4 | ต้องมีอย่างน้อย 1 ตารางจากข้อ 1-4 มีข้อมูลจริงไหลเข้า Sheets แล้ว |
| 6 | `FEATURE-dashboard-home-page.md` + `FEATURE-department-pages.md` | ต้องรอ 5 | ต้องมีข้อมูลจริงใน Supabase แล้วอย่างน้อย 1 ตาราง |

## กติกาที่คงไว้ตลอดทุกขั้นตอน (สรุปจากทุกไฟล์ อย่าฝ่าฝืน)

- **ห้ามเดา** selector, โครงสร้างชีต, ข้อความปุ่ม/label, ค่าที่เป็นไปได้ของ field ใดๆ — เปิดหน้าจริง/
  ไฟล์จริงตรวจสอบก่อนเขียนโค้ดทุกครั้ง ไม่มีข้อยกเว้น
- **ห้ามแตะ** `transfer-command-service.ts`, LINE bot, หรือ `.bat`/npm script ที่มีอยู่แล้ว จนกว่า spec
  จะบอกให้แก้ตรงๆ (มีจุดเดียวคือ `transfer-plan-service.ts` ตาม spec ข้อ 1)
- **เจอทางแยกที่ spec บอกว่า "ห้ามเดา ต้องถามผู้ใช้ก่อน"** (ดู checklist ใน `SYSTEM-ARCHITECTURE.md`
  หัวข้อ 7) → **หยุดแล้วถาม อย่าเดาไปเขียนต่อ** ทำเฉพาะส่วนที่ทำได้โดยไม่ต้องเดา แล้วรายงานว่าอะไรค้าง
  รอคำตอบ
- ทุก sheet/table ใหม่ที่เขียนต่อวัน ต้อง **upsert ไม่ใช่ append** (ต่อ runId+date+key ที่เกี่ยวข้อง)
  ตามที่ระบุไว้ในแต่ละไฟล์
- Export ทั้งชุดจากปุ่มในหน้าเว็บ ดีกว่า scrape DOM ทีละแถวเสมอ เช็คก่อนทุกครั้งว่ามีปุ่มนี้ไหม

## เมื่อทำแต่ละ step เสร็จ
รันตามหัวข้อ "วิธีทดสอบ" ในไฟล์ spec ของ step นั้นให้ผ่านก่อน ค่อยไป step ถัดไป — อย่าไล่เขียนทุก step
รวดเดียวแล้วเทสทีเดียวตอนท้าย เพราะจะหาไม่เจอว่า step ไหนพัง

## รายงานผลกลับมา
เมื่อจบแต่ละ step (หรือเจอจุดที่ต้องหยุดถาม) สรุปสั้นๆ ว่า: ทำอะไรไปแล้ว, ทดสอบผ่านหรือไม่, มีอะไรที่
เบี่ยงไปจาก spec เพราะเจอข้อมูลจริงไม่ตรงกับที่คาดไว้บ้าง (เช่น selector ไม่ตรงตามตัวอย่าง, field ที่
คาดว่าจะมีแต่ไม่มี) — สิ่งเหล่านี้สำคัญกว่าการบอกว่า "เสร็จแล้ว" เฉยๆ
