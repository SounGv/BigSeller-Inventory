# Feature: ตรวจสอบว่าใครทำ Stock-In เคสคืนสินค้าที่มีปัญหา (สูญหาย/ล้มเหลว) — เครื่องมือ audit ไม่ใช่ KPI อัตโนมัติ

Paste ไฟล์นี้ทั้งไฟล์เป็น prompt แรกให้ Cursor

## เป้าหมาย (ต่างจาก FEATURE-operator-picking-performance.md — อย่าปนกัน)
ต้องการ**เครื่องมือ audit แบบรันตามต้องการ** (ไม่ใช่ automation รายวัน) สำหรับตามรอยว่าใครเป็นคน
ดำเนินการ Stock-In ในเคสคืนสินค้าที่ "มีปัญหา" เท่านั้น — โดยเฉพาะสถานะ **"สูญหาย"** และ
**"การคืนสินค้าล้มเหลว"** — เพื่อใช้ตามงานเป็นรายเคส ไม่ใช่เอาไปทำเป็นตัวชี้วัดผลงานประจำ (ยืนยัน
จากผู้ใช้แล้ว: "อยากรู้เพื่อติดตามเคสเฉพาะที่มีปัญหา" ไม่ใช่ KPI)

**อย่ารวมเข้ากับ pipeline `DB_OPERATOR_PERFORMANCE`/scheduled task ที่ทำไปก่อนหน้านี้** เป็นคนละ
เรื่องกัน คนละจุดประสงค์

## แหล่งข้อมูล
หน้า `https://www.bigseller.com/web/order/inwarehouse/index.htm` ("การคืนสินค้า Stock-In")

ตัวกรอง "สถานะการคืนสินค้า" มีให้เลือกอยู่แล้ว: `รอคืนสินค้า`, `อยู่ระหว่างคืนสินค้า`, `คืนสินค้าแล้ว`,
**`การคืนสินค้าล้มเหลว`**, **`สูญหาย`**, `ยังไม่รับสินค้า` — กรองเฉพาะ 2 สถานะที่เป็นปัญหา (จำนวนน้อย
กว่ามาก เช่นตอนที่เห็นคือ สูญหาย 113 + ล้มเหลว 0 รวมกันหลักร้อย เทียบกับทั้งหมด 5311 — สเกลเล็กพอที่
จะเปิด detail ทีละรายการได้ถ้าจำเป็น ไม่ต้องกังวลเรื่อง performance เหมือนงานอื่นที่ผ่านมา)

## สิ่งที่ต้อง**ตรวจสอบก่อนเขียนโค้ด** (ห้ามเดา)

1. **คอลัมน์ "ผู้ซื้อ & ผู้ดำเนินการ" ในตาราง list จริงๆ แล้วโชว์อะไรบ้าง** — เปิดหน้าจริง กรอง
   สถานะ = สูญหาย แล้วดูว่าคอลัมน์นี้มีชื่อ/username พนักงานที่ทำ Stock-In โผล่มาด้วยไหม หรือมีแค่
   ชื่อผู้ซื้อ (ลูกค้า) อย่างเดียวแล้วต้องเปิด order detail ถึงจะเห็นผู้ดำเนินการจริง
2. **ถ้าไม่มีในหน้า list ต้องเปิด detail ทีละใบ** — เช็คว่ามีลิงก์ "ดูรายละเอียดคำสั่ง" หรือปุ่มอะไรที่
   พาไปหน้าที่มี audit trail ของ order นั้น (ใครกดอะไรตอนไหน) ก่อน ปริมาณน้อย (หลักร้อย) เปิดทีละใบ
   ได้โดยไม่กระทบ performance/ความเสี่ยงเรื่อง session เหมือนตอนพิจารณาหน้าออเดอร์ใหม่ (168 ใบ) ที่
   ผ่านมา
3. **เช็คว่ามีหมายเหตุ/remark ประกอบเคส "สูญหาย" ไหม** (เช่น สาเหตุที่สูญหาย, ใครรายงาน) — ถ้ามี ให้
   ดึงมาด้วย จะมีประโยชน์ต่อการตามเคสมากกว่าชื่อผู้ดำเนินการเฉยๆ

## งานที่ต้องทำ

### 1. Script แบบรันตามต้องการ: `scripts/audit-return-exceptions.ts`
**ไม่ต้องมี `.bat` หรือ Task Scheduler** — เป็นเครื่องมือที่พนักงาน/แอดมินรันเองตอนต้องการสืบเคส ไม่ใช่
งาน background

```typescript
// npx tsx scripts/audit-return-exceptions.ts [--status=สูญหาย|การคืนสินค้าล้มเหลว|all]
async function auditReturnExceptions(page: Page, statusFilter: string[]): Promise<void> {
  // 1. ensureSessionValid(page, RETURN_STOCK_IN_URL)
  // 2. ตั้งตัวกรอง "สถานะการคืนสินค้า" ตาม statusFilter
  // 3. อ่านทุกแถว: orderNo, sku, qty, returnStatus, buyerName, operator (ถ้ามีใน list),
  //    remark (ถ้ามี), dateStockIn/dateReturn
  // 4. ถ้า operator ไม่มีใน list ต้องเปิด detail เพิ่ม (ตามที่เช็คได้จากข้อ 2 ด้านบน)
  // 5. Export เป็น CSV/print ตาราง — ไม่ต้อง sync เข้า Google Sheets ถาวรก็ได้ เพราะเป็นการสืบ
  //    เฉพาะกิจ (ถามผู้ใช้ก่อนว่าอยากได้เป็น CSV ไฟล์เดียว หรืออยากเก็บสะสมใน sheet ด้วย)
}
```

## ห้ามทำ
- ห้ามตั้งเป็น scheduled task/automation — เป็นเครื่องมือ on-demand
- ห้ามเดาว่าคอลัมน์ "ผู้ดำเนินการ" อยู่ตรงไหน/หน้าไหน — ต้องเปิดหน้าจริงเช็คก่อนตามข้อ 1-3 ด้านบน
- ห้ามเอาไปรวมกับ `DB_OPERATOR_PERFORMANCE` ที่ทำไปก่อนหน้า

## คำถามที่ต้องถามผู้ใช้ก่อนเริ่ม (ถ้ายังไม่ได้ตอบ)
- อยากได้ผลลัพธ์เป็นไฟล์ CSV ต่อครั้งที่รัน หรืออยากสะสมไว้ใน Google Sheet ด้วย (เผื่อย้อนดูเคสเก่า)
- นอกจาก "สูญหาย" กับ "การคืนสินค้าล้มเหลว" มีสถานะอื่นที่ถือว่า "มีปัญหา" ต้องตามด้วยไหม (เช่น
  Stock-In บางส่วน ที่เห็นมี 1 รายการในภาพ)

## Addendum (2026-08-31): ฟีเจอร์นี้แทนที่ชีต "ออเดอร์ส่งผิด" เดิมด้วย (ไม่สร้างฟอร์มใหม่)
ทีมเดิมมีชีต `ออเดอร์ส่งผิด` ใน `รายงานของทีม.xlsx` ให้พนักงานพิมพ์เองตอนส่งของผิด — ตัดสินใจแล้วว่า
**ไม่สร้างฟอร์ม/ชีตใหม่แทนอันนี้** เพราะ BigSeller เองไม่มีทางรู้ตอนที่พนักงานหยิบผิดจริง (รู้ก็ต่อเมื่อ
ลูกค้าร้องเรียน/คืนของกลับมา) — เคสส่งผิดสุดท้ายจะโผล่เป็น**การคืนสินค้าใน BigSeller** อยู่ดี ซึ่งตรงกับ
ฟีเจอร์นี้พอดี ให้ audit tool นี้ทำหน้าที่แทนชีตเดิมไปเลย (เป้าหมายเดียวกัน: รู้ว่าใคร/เคสไหนมีปัญหา
แต่สืบจากข้อมูล BigSeller แทนการพิมพ์เอง) — ไม่ต้องทำอะไรเพิ่มในโค้ด เป็นแค่การยืนยัน scope
