# Feature: ดึงรายงานยอดขาย แยกออนไลน์/ออฟไลน์ จาก items.htm

Paste ไฟล์นี้ทั้งไฟล์เป็น prompt แรกให้ Cursor — เป็นงานแยกจาก
`FEATURE-pending-demand-and-offline-lock.md` (นั่นเรื่องสต็อก นี่เรื่องยอดขาย) แต่ใช้ข้อมูลเรื่อง
LockStock ชุดเดียวกัน — อ่าน Addendum 2 ในไฟล์นั้นประกอบก่อนเริ่ม

## เป้าหมาย
หน้า "เมนูยอดขาย" ต้องแยกดูได้ว่าเป็นยอดขาย**ออนไลน์**หรือ**ออฟไลน์** พร้อมกันของแท้ (ไม่ปน LockStock/
เคลม) — ใช้ native filter ที่ BigSeller มีให้อยู่แล้ว ไม่ต้องคำนวณกรองเอง

## แหล่งข้อมูล
หน้า `https://www.bigseller.com/web/statis/items.htm` แท็บ **"สรุปตาม SKU Merchant"**

ตัวกรอง "กลุ่มร้านค้า" มีตัวเลือกสำเร็จรูปตรงกับที่ต้องการอยู่แล้ว:
- `ONLINE SHP TIK LAZ` — ยอดออนไลน์ (Shopee/TikTok/Lazada รวมกัน)
- `OFFLINE ALL` — ยอดออฟไลน์ทั้งหมด
- `ALL ไม่รวม lockstock,เคลม` — ยอดรวมจริงทั้งบริษัท ไม่ปน LockStock และเคลม (ใช้เป็นเลขอ้างอิงรวม)

**ต้องเปิดหน้าจริงตรวจสอบก่อนเขียนโค้ด (ห้ามเดา):**
- ตัวกรองนี้เป็น checkbox เลือกได้หลายอันพร้อมกันไหม หรือเลือกได้ทีละอัน (ถ้าเลือกหลายอันพร้อมกันได้
  ต้องเช็คว่ามันรวมยอดหรือคำนวณซ้อนกันยังไง)
- หลังติ๊กเลือกต้องกดปุ่ม "ยืนยัน" ถึงจะ apply filter (เห็นในภาพตัวอย่างมีปุ่ม "ยืนยัน"/"ยกเลิก")
- การ์ดสรุป 4 ใบบนสุด (`จำนวนสินค้า`, `ยอดขาย SKU Merchant ที่มีผล`, `การขายสินค้า`,
  `การขาย SKU Merchant ที่มีผล`) มีความหมายต่างกันอย่างไรจริงๆ — ต้อง hover คำอธิบาย (ไอคอน `?`
  ข้างชื่อการ์ด) ก่อนตัดสินใจว่าตัวไหนคือ "ยอดขาย" ที่ต้องการจริงๆ
- มีปุ่ม export (ไอคอนดาวน์โหลด เห็นในภาพ) ทั้งที่กราฟและที่ตาราง "ข้อมูลการขาย SKU Merchant"
  ด้านล่าง — เช็คว่า export ให้ข้อมูลอะไรบ้าง ก่อนตัดสินใจว่าจะ scrape การ์ดสรุปอย่างเดียวหรือดึง
  ตารางรายละเอียดด้วย

## งานที่ต้องทำ

### 1. Page object ใหม่: `src/bigseller/sales-items-report-page.ts`
ตาม pattern เดียวกับหน้าอื่น — เปิดหน้าจริงก่อนเขียน locator ทุกตัว:
- ตั้งช่วงวันที่แบบ **วันเดียว** (from = to = วันที่ต้องการ เช่น 30 ส.ค. - 30 ส.ค. ตามภาพตัวอย่าง)
  ไม่ใช้ preset "วันนี้/เมื่อวาน" ของหน้าเว็บเอง เพราะต้องการควบคุมว่าดึงข้อมูลของวันไหนแน่ๆ
- เลือกกลุ่มร้านค้า = `ONLINE SHP TIK LAZ` → กด "ยืนยัน" → อ่านการ์ดสรุป 4 ใบ
- ทำซ้ำเลือก = `OFFLINE ALL` → อ่านการ์ดสรุปอีกชุด
- (ถ้าต้องการเลขรวมทั้งบริษัทด้วย) ทำซ้ำเลือก = `ALL ไม่รวม lockstock,เคลม`

### 1b. ปัญหา "ยอดขายวันนี้ตัดยอดยังไง" — ต้องพิสูจน์ก่อน ห้ามเดา (เพิ่ม 2026-08-31)
ตัวเลขของวันที่ยังไม่จบ (เช่นดึง "วันนี้" ระหว่างวัน) ไม่ใช่ยอดขายจริงทั้งวัน — ใช้หลักการเดียวกับ
`FEATURE-operator-picking-performance.md`: **sync "เมื่อวาน" เป็นค่าทางการเสมอ ไม่ sync "วันนี้" เป็น
ค่าทางการ** แต่ที่ต่างจากกรณีนั้นคือ **ยังไม่รู้ว่าหลังเที่ยงคืนแล้ว ตัวเลขของเมื่อวานนิ่งทันทีไหม** —
ระบบ analytics บางระบบมี delay ประมวลผลอีกชั้น (เช่น payment settle ช้า ทำให้ยอดย้อนหลังขยับได้อีก
2-3 ชม.หลังเที่ยงคืน) — **ต้องพิสูจน์เชิงประจักษ์ก่อนตัดสินใจ schedule**:

1. ก่อน deploy จริง ให้รัน sync ของ "เมื่อวาน" (ช่วงวันที่ = to = เมื่อวาน) **สองครั้งห่างกันหลายชั่วโมง**
   ในเช้าเดียวกัน (เช่น 06:00 กับ 10:00) แล้วเทียบตัวเลข 4 การ์ดสรุปว่าเหมือนกันเป๊ะไหม
2. ถ้าเหมือนกัน → เมื่อวานนิ่งตั้งแต่เช้าแล้ว sync ครั้งเดียว/วันพอ (ตั้งเวลาได้ตามสบาย เช่น 07:00)
3. ถ้าไม่เหมือนกัน → ต้องมี re-sync ซ้ำ (เช่น sync ทุก 3 ชม. สำหรับข้อมูลของ "เมื่อวาน" ไปจนกว่าจะนิ่ง
   หรืออย่างน้อย sync ซ้ำอีกครั้งช่วงบ่าย) — เพราะ `upsert` ที่ออกแบบไว้แล้ว (ต่อ `runId + date +
   channelGroup`) รองรับการเขียนทับด้วยเลขที่ถูกต้องกว่าอยู่แล้ว ไม่ต้องแก้ schema เพิ่ม
4. **ห้ามสมมติคำตอบข้อ 2 หรือ 3 เอง** — ผลจากการทดสอบจริงเท่านั้นที่บอกได้ว่าต้อง schedule แบบไหน

### 1c. แยก "ยอดวันนี้ (ชั่วคราว)" ออกจาก "ยอดที่ยืนยันแล้ว" ให้ชัดในข้อมูล
ถ้าต้องการโชว์ "ยอดขายวันนี้" ในหน้า home (ตาม `FEATURE-dashboard-home-page.md` การ์ดยอดขาย) ควรดึง
แบบ live เป็นค่า**ชั่วคราว**แยกต่างหาก ไม่ปนกับค่า `DB_SALES_SUMMARY` ที่เป็นค่าทางการของวันที่ปิดแล้ว
— เพิ่ม field `isProvisional: boolean` หรือเก็บคนละ sheet/key ก็ได้ (เช่น `DB_SALES_SUMMARY_LIVE`
แยกจาก `DB_SALES_SUMMARY`) เพื่อไม่ให้ตัวเลขชั่วคราวไปปนกับรายงานสรุปที่ต้องการความแม่นยำ (เช่น
หน้า "ยอดขาย" ที่กรองช่วงวันที่ย้อนหลังใน `FEATURE-department-pages.md` ต้องอ่านจาก `DB_SALES_SUMMARY`
เท่านั้น ไม่ปนกับค่า live)

### 2. Service ใหม่: `src/services/sales-summary-service.ts`
เขียนลงชีตใหม่ `DB_SALES_SUMMARY`:
`runId, date, channelGroup (online|offline|all_excl_lockstock), productCount,
effectiveSkuSalesValue, productSalesCount, effectiveSkuSalesCount, sourceUrl`

upsert ต่อ `runId + date + channelGroup` ไม่ append ซ้ำ

### 3. ต่อเข้า dashboard (ถ้าต้องการ)
ถ้าจะโชว์ในหน้า home ตามที่ออกแบบไว้ใน `FEATURE-dashboard-home-page.md` — เพิ่มเป็นการ์ดแยก
"ยอดขายออนไลน์วันนี้" กับ "ยอดขายออฟไลน์วันนี้" คู่กัน ไม่รวมเป็นก้อนเดียว เพราะจุดประสงค์คือให้
เทียบสัดส่วนสองช่องทางได้ ไม่ใช่แค่ดูยอดรวม

## ห้ามทำ
- ห้ามคำนวณกรอง LockStock เองอีก — ใช้ native filter `ALL ไม่รวม lockstock,เคลม` แทนเสมอเมื่อ
  ต้องการเลขที่ไม่ปน LockStock/เคลม
- ห้ามเดาความหมายของการ์ดสรุป 4 ใบ — ต้อง hover ดูคำอธิบายจริงจากไอคอน `?` ก่อน
- **ห้ามตั้ง schedule รันครั้งเดียวต่อวันจนกว่าจะทำการทดสอบข้อ 1b เสร็จแล้ว** — ถ้ายังไม่ทดสอบว่า
  "เมื่อวาน" นิ่งเมื่อไหร่ ห้ามเดา schedule time เอง
- ห้ามปนตัวเลข "วันนี้ (ชั่วคราว)" กับ `DB_SALES_SUMMARY` (ค่าทางการ) ในตารางเดียวกันโดยไม่มี field
  แยกบอกสถานะ (ดูข้อ 1c)

## วิธีทดสอบ
1. **ทำการทดสอบข้อ 1b ก่อนอื่นใด** — รัน sync ของ "เมื่อวาน" สองครั้งห่างกันหลายชั่วโมง เทียบตัวเลข
   ถ้าไม่นิ่งต้องปรับ schedule ตามที่ข้อ 1b ระบุ ก่อนจะถือว่างานนี้เสร็จ
2. เทียบตัวเลขที่ sync ได้กับที่เห็นบนหน้าเว็บจริงในช่วงวันที่เดียวกัน สำหรับทั้ง 2 (หรือ 3) กลุ่มร้านค้า
   ต้องตรงกันเป๊ะ

## Addendum (2026-09-02): สร้างเสร็จแล้วและทดสอบจริงแล้ว — ใช้ API ตรง ไม่ใช้ UI checkbox

**พบ 2 API ที่ใช้จริง (แทนการคลิก UI ที่พิสูจน์แล้วว่าไม่เสถียร — ดูด้านล่าง):**
- `GET /api/v1/shop/group/page.json` — คืนรายชื่อ "กลุ่มร้านค้า" ทั้งหมดพร้อม `id`, `groupName`,
  `shopIds` (ใช้แค่ `id` เพื่อความถูกต้อง — ดูหัวข้อบั๊กด้านล่าง)
- `POST /api/v1/skuSales/getSkuCountNew.json` — คืนตัวเลขสรุป 8 metric ต่อวัน
  (`salesCount, effectiveSalesAmount, salesVolumeCount, effectiveSalesQuantity, skuCount,
  refundsCount, refundsVolumeCount, efficientsOrders`) รับ `shopGroupIds, beginDate, endDate,
  currency, groupType`

**บั๊กที่เจอและแก้แล้วระหว่างพัฒนา:**
1. **UI checkbox "กลุ่มร้านค้าทั้งหมด" ติ๊กอยู่ default และไม่ auto-uncheck เมื่อเลือกกลุ่มอื่น** —
   ถ้าปล่อยไว้ตัวเลขจะรวมทุกกลุ่ม (ซ้ำ/ผิด) — จุดนี้ไม่กระทบโค้ดจริงเพราะตัดสินใจไม่ใช้ UI คลิกเลย
   (ดูข้อ 2) แต่บันทึกไว้เผื่อใครกลับไปใช้วิธี UI ในอนาคต
2. **UI checkbox dropdown นี้คลิกผ่าน Playwright ไม่เสถียรมาก** (element ไม่ visible สลับกันไปมา,
   `getByText('ยืนยัน')` ดันไปเจอปุ่มซ้ำที่ซ่อนอยู่ที่อื่นในหน้า) — **ตัดสินใจไม่ใช้ UI เลย** ใช้ 2 API
   ข้างต้นตรงๆ แทน (เรียก `shop/group/page.json` หา `id` ของกลุ่มที่ต้องการ แล้วส่งเข้า
   `getSkuCountNew.json` ตรงๆ) เร็วกว่า เสถียรกว่า ไม่ต้องพึ่ง UI เปลี่ยนแปลงเลย
3. **`shopGroupIds` รับ `id` ของกลุ่ม (เช่น `57014` สำหรับ "ONLINE SHP TIK LAZ") ไม่ใช่ list ของ
   `shopIds` สมาชิกในกลุ่ม** — ทดสอบแรกส่ง `shopIds` (17 ร้าน) ไปได้ผลลัพธ์ 0 ทุก metric ทุกวัน
   (สัญญาณชัดว่าผิด) แก้เป็นส่ง `id` เดี่ยวๆ แล้วได้ตัวเลขจริงถูกต้อง — ยืนยันด้วยการเช็คผลรวม:
   `online + offline = all_excl_lockstock` ตรงกันเป๊ะทั้ง 2 วันที่ทดสอบ (เช่น 01 ก.ย.:
   1,259,710.55 + 744,613.31 = 2,004,323.86) พิสูจน์ว่าไม่ซ้อนทับ/ไม่ตกหล่น

**ตาราง Supabase `sales_summary`** (`date, channel_group ['online'/'offline'/'all_excl_lockstock'],
sales_count, effective_sales_amount, sales_volume_count, effective_sales_quantity, sku_count,
refunds_count, refunds_volume_count, efficients_orders, is_provisional, source_url, synced_at`,
unique key `(date, channel_group)`)

**แก้แผน sync จากที่ระบุไว้เดิม (ข้อ 1b ด้านล่าง) — ไม่ต้องทำการทดสอบ empirical ก่อนแล้ว:**
เดิมสเปกให้ทดสอบว่า "เมื่อวาน" นิ่งกี่โมงก่อนตัดสินใจ schedule — **แทนที่ด้วย rolling window แทน**
sync ย้อนหลัง 3 วันทุกรอบ (`ROLLING_WINDOW_DAYS = 3`) แล้ว upsert ทับด้วย key `(date, channel_group)`
เสมอ — ถ้าตัวเลขวันเก่าเปลี่ยน (เช่น payment settle ช้า) รอบ sync ถัดไปจะแก้ให้เองอัตโนมัติ ไม่ต้องรอ
พิสูจน์ก่อนว่า "นิ่งเมื่อไหร่" — `is_provisional = true` เฉพาะวันนี้ (offset=0), วันอื่นเป็น `false`

โค้ด: `src/bigseller/sales-items-report-page.ts`, `src/services/sales-summary-service.ts`,
`scripts/sync-sales-summary.ts` (`npm run sync:sales-summary`) — standalone script เหมือนตัวอื่น
ยังไม่ผูกเข้า `syncBigSeller()` อัตโนมัติ

**ทดสอบจริงแล้ว (2026-09-02):** sync 3 วัน × 3 กลุ่ม = 9 แถว เขียนเข้า Supabase สำเร็จ ตัวเลขตรวจสอบ
ผลรวมถูกต้อง (ดูข้อ 3 ด้านบน)

## Addendum (2026-08-31): ยืนยันแนวทางถูกต้องแล้วจากไฟล์ทีมเดิม
ตรวจ `รายงานของทีม.xlsx` (ชีต `รายงานคำสั่งชื้อ ออฟไลน์`) พบว่าชื่อร้านที่ทีมพิมพ์เองในชีตเดิมคือชื่อ
ร้านค้า/ช่องทางที่มีอยู่แล้วใน BigSeller (ไม่ใช่แหล่งข้อมูลอื่นนอกระบบ) — ยืนยันว่าฟีเจอร์นี้ตามที่ออกแบบ
ไว้ (ดึงยอดออฟไลน์จาก native filter `OFFLINE ALL` ของ `items.htm` โดยตรง) ถูกทางแล้ว ไม่ต้องพึ่งชีต
พิมพ์เองอีกต่อไป ไม่มีการเปลี่ยนแผน implementation จากข้อนี้

