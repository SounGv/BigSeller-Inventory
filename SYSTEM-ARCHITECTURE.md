# GV Operations Data Platform — สถาปัตยกรรมระบบฉบับรวม

เอกสารนี้เป็น**จุดเข้าหลัก** สรุปทุกอย่างที่คุยกันมาทั้งเซสชัน แทนที่ `MASTER-plan-operations-data-
platform.md` และ `BUILD-sequence-consolidated.md` (สองไฟล์นั้นยังเก็บไว้ได้ แต่เนื้อหาถูกรวมมาไว้
ที่นี่ครบแล้ว) — ไฟล์ `FEATURE-*.md` แต่ละอันยังเป็นแหล่งรายละเอียดจริงสำหรับ Cursor ใช้ตอน build
เอกสารนี้บอกว่าแต่ละชิ้นต่อกันตรงไหนและทำไม

## 1. หลักการที่ยึดตลอดทั้งระบบ

- **ต่อยอด repo `BigSeller-Inventory` เดิม ไม่สร้างระบบคู่ขนาน** — ยกเว้นเว็บแดชบอร์ด (คนละ repo
  เพราะเป็นเว็บ ไม่ใช่ automation script)
- **ห้ามเดา** — selector, โครงสร้างชีต, ข้อความปุ่ม/label ทุกอย่างต้องเปิดของจริงตรวจสอบก่อนเขียนโค้ด
  เสมอ (เป็น convention ที่พิสูจน์แล้วว่าจำเป็นจริง — ดูหัวข้อ 5 "กับดักที่เจอมาแล้ว")
- **export ทั้งชุด ดีกว่า scrape DOM ทีละแถว** เมื่อหน้านั้นมีปุ่ม export ให้เช็คก่อนเสมอ
- **ระบบสั่งงานจริงที่ใช้งานอยู่ทุกวัน (`transfer-command-service.ts` + LINE bot) ห้ามแตะ** จนกว่า
  ของใหม่จะพิสูจน์ตัวเองแล้วว่าเสถียร

## 2. สถาปัตยกรรม 3 ชั้น

```
┌─────────────────────────────────────────────────────────────────┐
│ ชั้น 1: Data Layer                                                │
│ Playwright scraper (BigSeller-Inventory repo) → Google Sheets     │
│ (ของเดิม + ของใหม่ที่ออกแบบวันนี้) → ค่อยๆ ย้ายฝั่งรายงานไป Supabase │
└─────────────────────────────────────────────────────────────────┘
                    │                              │
                    ▼                              ▼
┌───────────────────────────────┐   ┌───────────────────────────────┐
│ ชั้น 2: Command Layer (มีอยู่แล้ว) │   │ ชั้น 3: Reporting Layer (ใหม่)   │
│ transfer-command-service.ts    │   │ เว็บแดชบอร์ด React/Vite         │
│ + LINE bot → สร้างใบย้ายจริง     │   │ อ่านจาก Supabase + Realtime     │
│ ใน BigSeller                    │   │ deploy Vercel free tier         │
└───────────────────────────────┘   └───────────────────────────────┘
```

## 3. คลังข้อมูลทั้งหมด (ของเดิม + ใหม่) ต่อหน้า BigSeller ที่เป็นแหล่งที่มา

| Sheet/Table | มาจากหน้า BigSeller | ใช้ทำอะไร | สถานะ |
|---|---|---|---|
| `DB_LOCATION_CURRENT`, `DB_SKU_INVENTORY` | `order/index.htm` (คลังสินค้า) | ตำแหน่ง/จำนวนสต็อกปัจจุบัน | มีอยู่แล้ว |
| (ไม่มีชื่อชีตระบุในแชท) | หน้ารายงานยอดขาย (sync-sales.ts เดิม) | ยอดขายที่ใช้อยู่แล้วก่อนเซสชันนี้ | มีอยู่แล้ว — **ยังไม่เคยถามว่า URL ไหนกันแน่** (ดูหัวข้อ "ยังไม่ชัด" ด้านล่าง) |
| `DB_SKU_CARTON_QTY` | นำเข้าเอง (ไม่ scrape) | จำนวนต่อลัง สำหรับปัดใบย้าย | มีอยู่แล้ว |
| `DB_TRANSFER_PLAN`, `DB_TRANSFER_EXCEPTIONS` | คำนวณจากข้อมูลข้างต้น | แผนย้าย/เคสที่ย้ายไม่ได้ | มีอยู่แล้ว |
| `DB_PENDING_ORDER_DEMAND` | `order/index.htm?status=new` | ออเดอร์ออนไลน์ที่ยังไม่ยืนยัน | ออกแบบแล้ว → `FEATURE-pending-demand-and-offline-lock.md` |
| `DB_OFFLINE_LOCK` | หน้าเดียวกัน กรอง store=LockStock | ของที่ล็อกขายหน้าร้าน แยก `lockStatus` | ออกแบบแล้ว (ไฟล์เดียวกัน) |
| Decoy reconciliation | เทียบ `STOCK_ซิงก์ขายออนไลน์` กับตำแหน่งชำรุดใน STOCK_5 | จับ data drift ของคลังหลอก | ออกแบบแล้ว (addendum ในไฟล์เดียวกัน) |
| `DB_OPERATOR_PERFORMANCE` | `statis/packagedAnalytics.htm` | ผลงานหยิบต่อคนต่อวัน เทียบเป้า | ออกแบบแล้ว → `FEATURE-operator-picking-performance.md` (รอชีตเป้าจริง) |
| Return-exception audit | `order/inwarehouse/index.htm` | ใครทำ Stock-In เคสสูญหาย/ล้มเหลว | ออกแบบแล้ว → `FEATURE-return-exception-audit.md` (on-demand) |
| `DB_SALES_SUMMARY` | `web/statis/items.htm` (แท็บ "สรุปตาม SKU Merchant") | ยอดขายออนไลน์/ออฟไลน์ แยกกัน (มี native filter ตัดเลข LockStock ให้เอง) | ออกแบบแล้ว → `FEATURE-sales-online-offline-report.md` |
| ใบโอนค้างระหว่างทาง | `web/inventory/transfer/index.htm` แท็บ "ระหว่างทาง" | แจ้งเตือนของค้างไม่กดรับนานผิดปกติ | ออกแบบแล้ว → addendum ใน `FEATURE-dashboard-home-page.md` (รอ threshold ชั่วโมง) |
| ออเดอร์ยกเลิกหลังแพ็ค | `order/index.htm?status=In+Cancel` | กันของหลุดส่งไปกับขนส่งทั้งที่ลูกค้ายกเลิกแล้ว | ออกแบบแล้ว (ไฟล์เดียวกัน, รอตรวจค่าคอลัมน์ "สถานะก่อนยกเลิก" จริง) |
| `DB_CHANNEL_SLA` | เอกสาร platform (ไม่ใช่หน้า BigSeller — มีอยู่แล้วนอกระบบ) | cutoff เวลาส่งแต่ละช่องทาง เรียงคิวใบย้าย | พูดถึงแล้ว ยังไม่ทำ |
| ฝ่ายออฟไลน์หยิบจัดออเดอร์ | **ยังไม่รู้หน้า** | ผลงานฝ่ายออฟไลน์ | รอสำรวจ |
| ฝ่ายคลัง+รับเข้า (ฝ่ายเดียวกัน — ยืนยัน 2026-08-31 จาก `รายงานของทีม.xlsx` ชีต `รับเข้า+คลัง`) | **ยังไม่รู้หน้า** | ผลงานรับเข้า/ตรวจนับ | รอสำรวจ |
| Wave report dashboard | `statis/...` (ไม่ได้จด URL แน่ชัด) "แผงรายงาน Wave" | เคยดูเป็นตัวอย่าง "อันดับการบรรจุวันนี้" | **ไม่ต้อง sync** — เป็น real-time snapshot วันนี้เท่านั้น ไม่มีย้อนหลัง ตกลงข้ามไปแล้ว |
| `web/log/index.htm` (operation log) | บันทึกการดำเนินการทุกแอคชัน 180 วัน | เคยพิจารณาเป็น fallback หาข้อมูลแพ็ค/รับเข้าถ้าไม่มีหน้ารายงานสำเร็จรูป | **ยังไม่ใช้จริง** — สำรองไว้เผื่อฝ่ายคลัง/รับเข้าไม่มีหน้ารายงานอื่นเลย |

## 4. เว็บแดชบอร์ด (ชั้น Reporting)

- **Data source:** Supabase (migrate เฉพาะตารางฝั่งรายงานจาก Sheets — ไม่แตะ `DB_TRANSFER_PLAN`)
  → `FEATURE-web-dashboard-supabase.md`
- **หน้า home:** จุดโฟกัสให้หัวหน้าฝ่าย — แถบ "ต้องดูก่อน" (รวม exception จากทุกแหล่ง เรียงตาม
  ความเร่งด่วน) + การ์ดเป้าต่อฝ่าย (time-adjusted ไม่ใช่เป้าคงที่ทั้งวัน) + placeholder การ์ดฝ่ายที่
  ยังไม่มีข้อมูล → `FEATURE-dashboard-home-page.md`
- **สไตล์ UI อ้างอิง:** ภาพตัวอย่างจาก BigSeller เอง (donut chart ตรงกลางมีเลขรวม, การ์ดเทียบเมื่อวาน
  เป็น %, ranking list มีแถบสี) — ยังไม่ได้ผูกเป็นข้อกำหนดบังคับใน spec ไฟล์ ถ้าต้องการให้ระบุชัดเจน
  ขึ้นบอกได้

## 5. กับดักที่เจอมาแล้ว — ต้องระวังซ้ำ (สรุปรวมไว้ที่เดียว กันลืม)

| กับดัก | จุดที่กระทบ | วิธีป้องกัน |
|---|---|---|
| Session หลุดเพราะ browser fingerprint ไม่ตรงกันระหว่าง login กับ sync | ทุก automation | ใช้ `launchBigSellerBrowser()` เดียวกันทุกจุด ห้าม bypass |
| ปุ่ม modal เปลี่ยนภาษาตาม locale (`No Prompt` → `ไม่เตือน`) | region-redirect popup | match ทั้งสองภาษาเสมอ ไม่ hardcode ภาษาเดียว |
| คลังหลอก `STOCK_ซิงก์ขายออนไลน์` mirror ของชำรุด | ทั้งสต็อกพร้อมขาย **และยอดขาย/อันดับร้านค้า** | กรอง warehouse/store นี้ออกทุกจุดที่คำนวณเลขจาก BigSeller โดยตรง ไม่เชื่อเลขสำเร็จรูปเฉยๆ |
| LockStock มี 2 สถานะปนกัน (ยืนยันแล้ว vs รอคอนเฟิร์ม) แยกแค่จากข้อความ ไม่มี field แยก | การหักลบสต็อก | ต้องอ่านข้อความ remark เอง ไม่มี field สำเร็จรูปให้เชื่อ |
| ข้อมูล SKU/จำนวนบางหน้าต้องเปิด detail ทีละใบ บางหน้าเห็นใน list เลย | ทุกหน้าที่ scrape ใหม่ | เช็คหน้า list ก่อนเสมอ อย่าสมมติว่าต้องเปิด detail จนกว่าจะพิสูจน์แล้ว |
| **(ใหม่ 2026-08-31)** Popup โปรโมทหมุนเวียนเนื้อหา (`.ant-modal-wrap.notice_dashboard`, เนื้อหาอยู่ใน `<iframe id="noticeContent">`) โผล่ไม่แน่นอน ปุ่มปิดข้อความไม่คงที่ ("ปิด"/"ต่อไป"/"ตกลง" แล้วแต่โปรโมท) | หน้า `order/index.htm` (อย่างน้อย) — สงสัยว่าอาจเจอหน้าอื่นด้วย | ใช้ `src/bigseller/order-page-overlays.ts` (`dismissOrderPageOverlays`) ลองทุก label ที่เจอแล้ว วนซ้ำก่อนคลิกจริงทุกครั้ง |
| **(ใหม่ 2026-08-31)** Onboarding tour การ์ด ("ยินดีต้อนรับเข้าสู่คู่มือปฏิบัติการ...") ปุ่ม "ข้าม"/"เริ่ม" — คนละระบบกับ notice_dashboard | หน้า `order/index.htm` (อย่างน้อย) | จัดการรวมอยู่ใน `dismissOrderPageOverlays` เดียวกัน |
| **(ใหม่ 2026-08-31)** `.language_switch_guide_mask` (ตัวเดียวกับที่ `dismiss-language-guide.ts` จัดการอยู่แล้วในหน้าคลังสินค้า) โผล่ในหน้า order ด้วย แต่ตอนแรกลืมเรียกใช้ ทำให้คลิกไปโดนเมนูบัญชีผู้ใช้แทนเงียบๆ | ทุกหน้าที่มี top nav (สังเกตว่าอาจเป็นทุกหน้าในระบบ ไม่ใช่แค่คลังสินค้า) | ใช้ `clickThroughGuide()` (ไม่ใช่ `.click()` ตรงๆ) กับทุกคลิกที่ใกล้ top nav ในทุกหน้า ไม่ใช่แค่หน้าที่เจอปัญหามาก่อน |
| **(ใหม่ 2026-08-31)** ตารางบางหน้า (`order/index.htm?status=new` และดูเหมือน `packagedAnalytics.htm` ด้วย) ไม่ใช้ `<tbody><tr>` ปกติ — หน้า order ใช้ `<table class="list_items">` แยกตารางต่อ 1 ออเดอร์ และแม้ตั้ง page size สูงสุดแล้ว ตารางก็ render แบบ lazy/virtual (เห็นแค่แถวใกล้ scroll position ไม่ใช่ทั้งหน้า) ต้อง scroll ไล่เก็บสะสม ไม่ใช่ query ทีเดียวจบ | `order/index.htm?status=new`, สงสัยว่า `packagedAnalytics.htm`/`order/inwarehouse/index.htm` ด้วย (ยังไม่ยืนยัน) | ตรวจ DOM จริงก่อนเขียน parser ทุกครั้ง อย่าสมมติว่า `tbody tr` ใช้ได้ทุกหน้า — ดู `collectOrdersOnCurrentPage()` ใน `new-orders-page.ts` เป็นตัวอย่างวิธี scroll-and-collect |
| **(ใหม่ 2026-08-31)** Element ที่มี class ซ้ำกันหลายจุดในหน้าเดียว (เช่น `.pagination` โผล่ 2-3 รอบ) — locator ที่ไม่ใส่ `.first()`/scope ให้แคบพอ จะโดน Playwright strict-mode error ซึ่งถ้ามี `.catch(() => undefined)` ครอบไว้จะเงียบหายไปเฉยๆ ดูเหมือนทำงานได้แต่จริงๆ ไม่ได้ทำอะไรเลย | ทุกหน้าที่มี layout ซ้ำ (เช่น pagination บนล่างตาราง) | ห้ามใส่ `.catch(() => undefined)` ครอบ action สำคัญ (เช่น เปลี่ยน page size) แบบเงียบๆ — ถ้า fail ต้องรู้ทันที ไม่ใช่ค้นพบทีหลังว่าตัวเลขผิด |

## 6. ลำดับ build (คงตามที่วางไว้ใน BUILD-sequence-consolidated.md)

1. `FEATURE-pending-demand-and-offline-lock.md` (รวม decoy) — แก้ `transfer-plan-service.ts` จุดเดียว
2. `FEATURE-operator-picking-performance.md` — ทำคู่ขนานกับข้อ 1 ได้ รอชีตเป้าก่อนต่อส่วนเทียบเป้า
3. `FEATURE-return-exception-audit.md` — อิสระ ทำเมื่อไหร่ก็ได้
4. `FEATURE-web-dashboard-supabase.md` — เริ่มได้เมื่อข้อ 1 หรือ 2 มีข้อมูลจริงแล้วอย่างน้อย 1 ตาราง
5. `FEATURE-dashboard-home-page.md` (frontend) — เริ่มได้เมื่อข้อ 4 มีข้อมูลจริงใน Supabase แล้ว

*(ไม่ต้องทำ `common-controls.ts` เป็นชั้น framework ใหญ่ — ดึงเฉพาะ UI mechanics ที่ซ้ำจริง เป็น
helper บางๆ พอ ตามที่คุยกันไปแล้ว)*

## 7. สิ่งที่ต้องตัดสินใจ/หาข้อมูลเพิ่ม ก่อนงานจะครบทุกฝ่าย

**สถานะหลังคุยรอบ 2026-08-31 (ดู `รายงานของทีม.xlsx` ที่ผู้ใช้ส่งมา):**

- [x] ~~Spreadsheet ID/ชื่อชีต/โครงสร้างคอลัมน์ของชีตเป้าที่ใช้อยู่ใน team-kpi-workbooks~~ → **ตัดสินใจ
  ไม่ join กับชีตเดิม** เพราะพนักงานต้องพิมพ์เอง ลืมพิมพ์แล้วไม่มีข้อมูล (ตรวจแล้ว template สำรวจว่าง
  เปล่าจริง) ใช้เป้าคงที่ + ข้อมูล BigSeller ล้วนแทน (ดู `FEATURE-operator-picking-performance.md`)
- [x] ~~เป้าต่อวันของบทบาทอื่นนอกจากออนไลน์-หยิบ (300 พัสดุ/วัน)~~ → แก้เป็น **350/คน/วัน** สำหรับ
  ออนไลน์-หยิบ (ตัวเลขจริงจากไฟล์เดิม) เป้าฝ่ายอื่นยังไม่มีในไฟล์ที่ส่งมา รอข้อมูลเมื่อฝ่ายนั้นเชื่อมต่อ
- [x] ~~ออเดอร์ส่งผิด~~ → ไม่สร้างฟีเจอร์ใหม่ ใช้ `FEATURE-return-exception-audit.md` แทน (คนละชื่อ
  แต่จุดประสงค์เดียวกัน สืบจาก BigSeller แทนพิมพ์เอง)
- [x] ~~ปัญหาหน้างาน (IT/facility)~~ → ตัดออกจาก scope ทั้งหมด ไม่เกี่ยวกับ inventory/sales
- [x] ~~ฝ่ายรับเข้า vs ฝ่ายคลัง แยกกันไหม~~ → เป็นฝ่ายเดียวกัน (มีชีต `รับเข้า+คลัง` ในไฟล์เดิมยืนยันอยู่แล้ว)
- [x] ~~เวลาเข้า-ออกงาน~~ → ตัดออกจากระบบใหม่ทั้งหมด ไม่ดึงมา

**ยังไม่มีคำตอบ (ไม่บล็อกการเริ่ม Step 1):**
- [ ] Screenshot หน้ารายงานฝ่ายออฟไลน์หยิบจัดออเดอร์ (ถ้ามี)
- [ ] Screenshot หน้ารายงานฝ่ายคลัง+รับเข้า (ถ้ามี)
- [ ] สูตร "เป้าที่ปรับตามเวลา" สำหรับหน้า home (เส้นตรงตามกะ / เฉลี่ยย้อนหลัง / อื่นๆ) — ยืนยันแล้วว่า
  ไม่ใช้เวลาเข้างานจริง ต้องอิงกะมาตรฐานแทน แต่สูตรละเอียดยังไม่ตกลง
- [ ] เอกสาร SLA/cutoff จาก platform (มีแล้ว แค่รอ digitize)
- [ ] เกณฑ์สีแดง/เหลือง/เขียวของการ์ดเป้า (เสนอ 70%/100% ไว้ก่อน ปรับได้)

## 8. ระบบแรงกิ้ง (เพิ่ม 2026-08-31 — ไม่มีใน draft แรก)

เพิ่มฟีเจอร์ leaderboard ต่อฝ่าย บนหน้า home และหน้า drill-down แต่ละฝ่าย — จัดอันดับจากจำนวนพัสดุ/ชิ้น
ที่หยิบ-แพ็คอย่างเดียว แยกลิสต์ต่อฝ่าย (ไม่รวมข้ามฝ่าย) ไม่โชว์คนต่ำกว่าเป้าในลิสต์ (ไปที่แถบ "ต้องดูก่อน"
แทน) รายละเอียดเต็มอยู่ใน addendum ของ `FEATURE-dashboard-home-page.md` และ
`FEATURE-department-pages.md`

## 9. Scheduler ของ scraper ใหม่ทั้งหมด (ยืนยัน 2026-08-31)

แต่ละ scraper ใหม่ (pending-demand, operator-performance, sales-summary) มีสคริปต์/schedule แยกกันเอง
เหมือน pattern เดิมของโปรเจกต์ (`session-keeper.ts`/sync/transfer-plan แยกกันอยู่แล้ว) — **ไม่รวมเป็น
runner เดียว** เพราะแต่ละ Step ทยอยเสร็จไม่พร้อมกัน (ตกลงว่าเปิดใช้งานทีละ Step ทันทีที่เสร็จ ไม่รอ
เขียนรวด) แค่ตั้งเวลาให้แต่ละสคริปต์ไม่ชนกัน (stagger) พอ
