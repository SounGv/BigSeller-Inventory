# โครงสร้างเมนู — แยกหน้าตามฝ่าย + ตัวกรองบุคคล/ช่วงวันที่ (ต่อจาก FEATURE-dashboard-home-page.md)

หน้า **home** (`FEATURE-dashboard-home-page.md`) ยังเป็นจุดโฟกัสภาพรวมเหมือนเดิม — ไฟล์นี้เพิ่มหน้า
**drill-down แยกตามฝ่าย** ที่กดเข้าไปดูรายละเอียดได้จากหน้า home

## เมนูหลัก (sidebar)

```
🏠 ภาพรวม (home — ตาม FEATURE-dashboard-home-page.md)
📦 ฝ่ายคลัง+รับเข้า
💻 ฝ่ายออนไลน์
🏬 ฝ่ายออฟไลน์
💰 ยอดขาย
```

**แก้ 2026-08-31: รวม "ฝ่ายคลัง" กับ "ฝ่ายรับเข้า" เป็นเมนูเดียว** — ตรวจ `รายงานของทีม.xlsx` (ไฟล์
รายงานทีมเดิม) พบชีตชื่อ `รับเข้า+คลัง` อยู่แล้ว ยืนยันว่าในทางปฏิบัติทีมนี้เป็นฝ่ายเดียวกัน ไม่ต้องแยก
หน้า — ลด 5 เมนูเหลือ 4 เมนู (ไม่รวมภาพรวม)

แต่ละเมนู (ยกเว้นภาพรวม) เป็นหน้า **เดียวกันในเชิงโครงสร้าง** ต่างกันแค่แหล่งข้อมูล — ใช้ component
เดียวกันซ้ำ ไม่ต้องออกแบบ layout แยกทุกหน้า

## Layout มาตรฐานของหน้าแต่ละฝ่าย

### ตัวกรองบนสุด (ใช้ร่วมกันทุกหน้าฝ่าย)
- **ช่วงวันที่** — date range picker แบบ from-to (เช่น 1 ส.ค. 2026 → 31 ส.ค. 2026) ไม่ใช่แค่
  "วันนี้/เมื่อวาน" แบบหน้า home — ต้อง query ข้อมูลตามช่วงที่เลือกจริง ไม่ hardcode
- **บุคคล** — dropdown เลือกดูทั้งฝ่าย หรือเจาะรายคน (ใช้ค่า `operator` จาก
  `DB_OPERATOR_PERFORMANCE`/ตารางที่เกี่ยวข้องของฝ่ายนั้น)

### เนื้อหาหน้า (ต่อฝ่าย/ต่อคน ตามตัวกรองที่เลือก)
- ตัวเลขจริง vs เป้า ในช่วงวันที่เลือก (รวม + แยกรายวันเป็นกราฟเส้น/แท่ง)
- รายการงานค้าง/exception เฉพาะฝ่ายนั้นในช่วงวันที่เลือก
- ถ้าเลือกดูรายคน (ไม่ใช่ทั้งฝ่าย) โชว์ trend ของคนนั้นแยก ไม่ใช่ค่าเฉลี่ยรวมฝ่าย

## แหล่งข้อมูลต่อเมนู (สถานะ ณ ตอนนี้)

| เมนู | ตาราง Supabase/Sheets ที่ใช้ | สถานะ |
|---|---|---|
| ฝ่ายออนไลน์ | `DB_OPERATOR_PERFORMANCE` (หยิบ) | มี spec แล้ว |
| ฝ่ายคลัง+รับเข้า | ยังไม่มีตาราง — รอสำรวจหน้า BigSeller (ตาม `SYSTEM-ARCHITECTURE.md` หัวข้อ 7) — ยืนยันแล้วว่าเป็นฝ่ายเดียวกัน (2026-08-31) ไม่ต้องแยกตารางตอนออกแบบ | รอข้อมูล |
| ฝ่ายออฟไลน์ | ยังไม่มีตาราง — รอสำรวจ | รอข้อมูล |
| ยอดขาย | `DB_SALES_SUMMARY` (จาก `FEATURE-sales-online-offline-report.md`) | มี spec แล้ว — **ต้องแก้ให้รองรับช่วงวันที่ตามต้องการ ไม่ใช่แค่วันนี้/เมื่อวาน** (ดูหมายเหตุด้านล่าง) |

**เมนูที่ยังไม่มีข้อมูล ให้ขึ้นหน้าเปล่าพร้อมข้อความ "ยังไม่เชื่อมข้อมูลฝ่ายนี้" แทนการซ่อนเมนูไปเลย** —
รักษาโครงสร้าง 5 เมนูไว้ตั้งแต่แรก จะได้ไม่ต้องแก้ nav ใหม่ทีหลัง

## ผลกระทบต่อ `FEATURE-sales-online-offline-report.md` — ต้องแก้ให้รองรับช่วงวันที่กำหนดเอง
สเปกเดิมออกแบบไว้แบบ sync รายวัน (เหมือน operator performance) — แต่หน้าเมนู "ยอดขาย" ต้องกรอง
ช่วงวันที่เองได้ตามต้องการ (เช่น 1-31 ส.ค.) ซึ่ง `items.htm` รองรับ custom date range อยู่แล้วในตัว
หน้าเว็บ (เห็นในภาพตัวอย่างมี date range picker) — สองทางเลือก:
1. **Sync ทุกวันเก็บสะสมเป็นรายวัน** (ตามสเปกเดิม) แล้วให้ frontend **รวมผลเอง** ตามช่วงที่ผู้ใช้เลือก
   จากข้อมูลรายวันที่มีอยู่แล้ว (Sum ฝั่ง query ไม่ต้อง scrape ใหม่ทุกครั้งที่เปลี่ยนช่วงวันที่)
2. หรือให้หน้าเว็บยิง scrape สดตามช่วงที่เลือกทุกครั้ง (ช้ากว่า พึ่ง BigSeller ตรงๆ)

**แนะนำทางเลือก 1** — sync สะสมรายวันไว้ใน Supabase เหมือนเดิม แล้วให้ frontend query แบบ
`SUM(...) WHERE date BETWEEN :from AND :to` — เร็วกว่า ไม่ต้องพึ่ง BigSeller ตอน user กรองวันที่ และ
ตรงกับหลักการ "ไม่โหลดช้า" ที่ตกลงกันไว้ตอนออกแบบ Supabase

## ห้ามทำ
- ห้ามซ่อนเมนูฝ่ายที่ยังไม่มีข้อมูล — โชว์เป็นหน้าเปล่าพร้อมข้อความแทน
- ห้ามให้หน้า "ยอดขาย" scrape สดทุกครั้งที่เปลี่ยนช่วงวันที่ (ช้า ขัดกับเป้าหมายเดิม) — ใช้ข้อมูลสะสม
  รายวันใน Supabase แล้ว query รวมเอาแทน

## Addendum (2026-08-31): ระบบแรงกิ้งต่อฝ่าย (ในช่วงวันที่ที่กรอง)
แต่ละหน้าฝ่าย (ที่มีข้อมูลแล้ว) เพิ่ม section "อันดับผลงานพนักงาน — ช่วงที่เลือก" ใต้กราฟจริง-vs-เป้า —
กติกาเดียวกับ ranking บนหน้า home (ดู `FEATURE-dashboard-home-page.md` addendum 2026-08-31): จัดอันดับ
จากจำนวนพัสดุ/ชิ้นที่หยิบ-แพ็คสะสมในช่วงวันที่ที่กรองไว้เท่านั้น ไม่ผสมฝ่ายอื่น ไม่โชว์คนที่ต่ำกว่าเป้า
ในลิสต์นี้

## Addendum (2026-08-31): จุดเริ่มต้นโค้ด frontend — ใช้ Mosaic Lite (Cruip) เป็นฐาน
ดูรายละเอียดที่ `FEATURE-web-dashboard-supabase.md` — ตรวจสอบแล้วว่า stack ตรงกับที่ออกแบบไว้
(React 19 + Vite 6 + Tailwind v4 + Chart.js) มี `Datepicker.jsx`/`DateSelect.jsx` ที่ตรงกับตัวกรอง
ช่วงวันที่ในไฟล์นี้พอดี ไม่ต้องเขียน component ใหม่
