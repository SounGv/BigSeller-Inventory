# หน้าแดชบอร์ดหลัก — จุดโฟกัสสำหรับหัวหน้าฝ่าย (ต่อจาก FEATURE-web-dashboard-supabase.md)

ไฟล์นี้เจาะจงหน้า **home/overview** ของเว็บแดชบอร์ด (Step 5 ใน `BUILD-sequence-consolidated.md`) —
ไม่ใช่หน้ารายละเอียดรายบุคคล/ราย SKU (หน้าพวกนั้นทำแยกเป็นหน้า drill-down ต่างหาก)

## โจทย์
หัวหน้าฝ่ายเปิดระบบมาต้อง **รู้ทันทีภายในสายตาเดียว** ว่า:
1. งานค้างอะไรบ้าง ของฝ่ายไหน
2. เป้าประจำวันแต่ละฝ่ายไปถึงไหนแล้ว
3. จุดไหนควรเข้าไปดู/กระตุ้นคน — ไม่ใช่แค่โชว์ตัวเลขดิบให้ไปนั่งแปลเอง

**หลักการออกแบบ: หน้านี้ต้องเป็น "สรุปที่ตัดสินใจได้ทันที" ไม่ใช่ตารางข้อมูลดิบ** — ทุก widget ต้อง
ตอบคำถาม "ต้องทำอะไรต่อ" ไม่ใช่แค่ "ตัวเลขคืออะไร"

## Layout หน้า home (บนลงล่าง เรียงตามความสำคัญ)

### 1. แถบบนสุด — "ต้องดูก่อน" (Attention Required)
รายการที่ดึงมาจากหลายแหล่งรวมกัน เรียงตามความเร่งด่วน ไม่ใช่แยกตามแหล่งข้อมูล:
- Operator ที่ผลงานวันนี้ต่ำกว่า **80% ของเป้า** ณ เวลาปัจจุบันของวัน (ต้องคำนวณเทียบสัดส่วนเวลาที่
  ผ่านไปในกะงาน ไม่ใช่เทียบเป้าเต็มวันตอนเที่ยง — ดูหัวข้อ "เป้าที่ปรับตามเวลา" ด้านล่าง)
- Transfer plan exception ที่ยังไม่ resolve (จาก `DB_TRANSFER_EXCEPTIONS`)
- Order ที่ BigSeller ขึ้น "ของขาด" ที่ยังไม่ได้จัดการ (จาก Step 1)
- เคสคืนสินค้า "สูญหาย"/"การคืนสินค้าล้มเหลว" ที่เพิ่งเกิดใหม่ (จาก Step 3, ถ้าเชื่อมแล้ว)
- Decoy warehouse reconciliation mismatch (จาก Step 1 addendum)
- **ใบโอนสินค้าที่ค้างอยู่ระหว่างทางนานผิดปกติ ยังไม่กดรับ** (เพิ่มใหม่ 2026-08-31 — ดูรายละเอียดข้อ 1b)
- **ออเดอร์ที่ถูกยกเลิกหลังจากแพ็คไปแล้ว เสี่ยงหลุดส่งไปกับขนส่ง** (เพิ่มใหม่ 2026-08-31 — ดูข้อ 1c)
- **ใบเติมสต็อกชั้นวาง/ย้ายสินค้าภายในคลัง ที่ค้างนานผิดปกติในสถานะกลางทาง** (เพิ่มใหม่ 2026-09-01 — ดูข้อ 1d)

แต่ละรายการ = 1 บรรทัด สั้นๆ กดแล้วพาไปหน้า detail ที่เกี่ยวข้องเลย ไม่ใช่ตาราง ไม่ใช่กราฟ

**ถ้าไม่มีอะไรต้องดู ให้โชว์ว่าง/สถานะปกติชัดเจน** (เช่น "✓ ไม่มีรายการต้องติดตามตอนนี้") — อย่าปล่อย
เป็นช่องว่างเฉยๆ เพราะหัวหน้าฝ่ายจะไม่แน่ใจว่าระบบทำงานอยู่ไหมหรือแค่ไม่มีข้อมูล

### 1b. ใบโอนสินค้าค้างระหว่างทาง (เพิ่มใหม่ 2026-08-31, ขยายความ 2026-09-01)
**แหล่งข้อมูล:** `https://www.bigseller.com/web/inventory/transfer/index.htm` แท็บ **"ระหว่างทาง"**
ครอบคลุมการโอนระหว่างคลังทุกคู่ (เช่น STOCK_12→STOCK_8, STOCK_8→STOCK_5, →STOCK_6 ฯลฯ ไม่จำกัดเฉพาะคู่ใดคู่หนึ่ง)

ความเสี่ยง: ใบโอนที่กดส่งจากคลังต้นทางแล้วแต่ปลายทางยังไม่กดรับ (Stock-In) — สต็อกจะหายไปจากคลัง
ต้นทางในระบบแต่ยังไม่เข้าคลังปลายทาง ถ้าไม่มีใครติดตามอาจเป็นของหายจริงหรือลืมกดรับเฉยๆ ก็ได้ ไม่มี
ทางแยกแยะได้จากตัวเลขเดียว

**แก้ไข 2026-09-01 (ยืนยันจากผู้ใช้) — ต้องติดตาม 2 ขั้น ไม่ใช่แค่ "กดรับหรือยัง":**
กดรับ (Stock-In) ที่คลังปลายทางแล้ว **ไม่ได้แปลว่าจบงาน** ของอาจถูกกดรับเข้าระบบแต่ยังไม่ถูกนำไปวางเก็บ
เข้าตำแหน่งจริง (ตกค้างอยู่หน้าคลัง/พื้นที่รับของ) ต้องมีสถานะแยก 2 ขั้นต่อใบโอน:

1. **กดรับแล้วหรือยัง** (Stock-In ที่คลังปลายทาง) — เช็คจากแท็บ "ระหว่างทาง" เหมือนเดิม (หายไปจากแท็บนี้ = กดรับแล้ว)
2. **เก็บเข้าตำแหน่งจริงแล้วหรือยัง** (หลังกดรับ) — **ยังไม่รู้ว่า BigSeller มีหน้า/สถานะที่บอกขั้นนี้ตรงๆ
   ไหม ต้องสำรวจก่อนเขียนโค้ด (ห้ามเดา)** แนวทางที่เป็นไปได้ (ยังไม่ยืนยัน): เทียบกับ `DB_LOCATION_CURRENT`
   ว่า SKU/จำนวนที่เพิ่งกดรับปรากฏในตำแหน่งของคลังปลายทางแล้วหรือยัง — ถ้า BigSeller มีสถานะ "รอเก็บเข้า
   ตำแหน่ง"/"เก็บแล้ว" ให้ใช้ตรงๆ แทนการเทียบเอง

**ยืนยันแล้ว 2026-09-01 (ตรวจสอบจริงกับ API):** คอลัมน์ "เวลาที่จะมาถึงโดยประมาณ" (`estimatedArrivalTime`
ใน API) เป็นค่าว่าง `""` ใน**ทุก**แถวจริง (37/37 รายการที่กำลังค้างอยู่ตอนตรวจสอบ) — ไม่เคยถูกกรอกจริงใน
ทางปฏิบัติ ยืนยันว่าต้องใช้ **threshold คงที่แทน** ตามที่คาดไว้ **ยืนยันแล้ว 2026-09-01: threshold = 24 ชม.** นับจาก `bigseller_created_at` — ข้อมูลจริง ณ ตอนตรวจสอบ:
ใบโอนที่ค้างนานที่สุดคือ 330 ชม. (~13.7 วัน, `TRUJR3197208` STOCK_5→STOCK_6) รองลงมา 280 ชม. (~11.7 วัน)

**ยืนยันแล้ว 2026-09-01 (รอบ 4): `receivedStatus` ไม่มี dropdown/staff attribution** — ขั้น "กดรับ"
เป็นการกระทำใน BigSeller เอง (ไม่ใช่หน้าเรา) เราแค่อ่านสถานะจากแท็บมาโชว์เฉยๆ ไม่ต้องให้พนักงานกดเช็คซ้ำ
ผ่าน dropdown ของเรา — มีแค่ `shelvedStatus` เท่านั้นที่เป็น manual checklist ผ่าน dropdown (เพราะ
BigSeller ไม่มีข้อมูลขั้นนี้ให้)

ดึงเข้า `DB_TRANSFER_EXCEPTIONS` หรือแยกเป็นตารางใหม่ `DB_TRANSFER_IN_TRANSIT` ก็ได้ (คอลัมน์:
`transferNo, sourceWarehouse, destWarehouse, createdAt, estimatedArrival, hoursInTransit,
receivedStatus, shelvedStatus, shelvedBy, shelvedAt`) — `receivedStatus` อ่านจาก BigSeller ตรงๆ
(ไม่มี `receivedBy`/`receivedAt`) ส่วน `shelvedBy`/`shelvedAt` คือพนักงานที่กดเช็คผ่าน dropdown ในระบบ
เราเอง (ดู addendum 2026-09-01 "การอ้างอิงตัวผู้ทำงาน")

**สร้างเสร็จแล้ว 2026-09-01 — ฝั่ง sync/backend ของ 1b พร้อมใช้งานจริง:**
- ตาราง Supabase `transfer_in_transit` สร้างแล้ว (คอลัมน์ตรงตามด้านบน แบบ snake_case, `hours_in_transit`
  **ไม่ได้เก็บเป็นคอลัมน์นิ่งๆ** เพราะจะค้างเก่าทันทีที่เวลาเดินต่อ — คำนวณสดตอน query แทนด้วย
  `extract(epoch from (now() - bigseller_created_at))/3600`)
- **Supabase-only ไม่มี Sheets** — ต่างจากทุก sync อื่นในโปรเจกต์นี้ เพราะตารางนี้มี `shelved_*` เป็น
  manual overlay ที่พนักงานกรอกผ่านหน้าเว็บ ถ้าใช้ `SheetsClient.upsertRows` (ซึ่งเขียนทับทั้งแถวทุก
  คอลัมน์ที่ระบุใน headers) จะไปเขียนทับค่าที่พนักงานกรอกไว้ทุกครั้งที่ sync รอบใหม่ — Supabase
  upsert เขียนทับเฉพาะคอลัมน์ที่ระบุใน payload เท่านั้น จึงปลอดภัยกว่าในเคสนี้
- `receivedStatus` ตรวจจับด้วยการ diff: ใบโอนที่เคย `received_status=false` แต่หายไปจากผลสแกน
  "ระหว่างทาง" รอบล่าสุด แปลว่าเพิ่งถูกกดรับ → auto flip เป็น `true` พร้อม `received_at` (ไม่มี by)
- ข้อมูลดึงผ่าน **API ภายในของ BigSeller ตรงๆ** (`POST /api/v1/inventory/transfer/pageList.json`,
  `status=1` = แท็บ "ระหว่างทาง") แทน DOM scraping — ยืนยันกับผู้ใช้แล้วว่าใช้แนวทางนี้ได้ (คืนค่า JSON
  มีโครงสร้างชัดเจน ไม่ต้อง parse วันที่ภาษาไทยเอง) ดู `src/bigseller/transfer-page.ts`
- `mark_transfer_shelved(p_transfer_no, p_staff_id)` — Postgres RPC (SECURITY DEFINER) เป็นทางเดียวที่
  frontend (anon key) เขียนได้ ตรวจสอบว่า staff id มีอยู่จริงและ active ก่อนเซ็ต `shelved_status/
  shelved_by/shelved_at` — ป้องกันไม่ให้เปิด UPDATE เต็มตารางให้ browser เขียนตรงๆ
- โค้ด: `src/bigseller/transfer-page.ts`, `src/services/transfer-in-transit-service.ts`,
  `scripts/sync-transfer-in-transit.ts` (`npm run sync:transfer-in-transit`) — **ยังไม่ได้ผูกเข้า
  `syncBigSeller()` อัตโนมัติ** ตั้งใจทำเป็น standalone script ก่อนแบบเดียวกับ `sync:order-demand`
  (ยังไม่ auto-run ทุกรอบ sync หลัก จนกว่าจะตัดสินใจอีกที)
- ทดสอบจริงแล้ว (2026-09-01): ดึงได้ 37 ใบโอนที่ค้างอยู่จริง เขียนเข้า Supabase สำเร็จ
- **แก้บั๊กจริง 2026-09-01:** `bigsellerCreatedAt` เพี้ยนไป 7 ชม. ทุกแถว (ค้นพบตอนทดสอบ UI จริงแล้วเห็น
  "ค้าง -4 ชม." ในหน้าเว็บ) — สาเหตุคือ epoch field (`createTime`) ของ BigSeller เองไม่ใช่ UTC จริง แต่เป็น
  เวลาไทย(ท้องถิ่น)ที่ serialize เป็น epoch โดยไม่แปลง timezone (บั๊กฝั่ง BigSeller ไม่ใช่ฝั่งเรา) ยืนยันด้วย
  การเทียบกับ timestamp log ของโปรเจกต์เอง (UTC จริง) พบว่า epoch ที่ decode ตรงๆ ให้เวลาที่ "อนาคต" กว่า
  ตอนที่สังเกตเห็นข้อมูลนั้นจริง ซึ่งเป็นไปไม่ได้ — แก้ด้วย `thaiEpochToUtcIso()` ใน `src/utils/thai-date.ts`
  (ลบ 7 ชม.) sync ใหม่ + backfill แถวเก่าที่ค้างจากบั๊กแล้ว (1 แถว) ตรวจสอบว่าค่าใหม่ถูกต้องแล้ว
- **สร้างเสร็จแล้วและทดสอบจริงแล้ว 2026-09-01 — UI checklist ใน GV Ops Console:**
  `src/pages/Checklist.jsx` (route `/checklist`) — dropdown เลือกชื่อ (แยกต่อ section ตามที่กรอง
  department ไว้ถูกต้อง) เรียก RPC `mark_transfer_shelved` ตรงๆ จาก frontend (anon key) ทดสอบจริงผ่าน
  Playwright แล้ว: เลือกชื่อ "St_ter" กดปุ่ม "เก็บเข้าตำแหน่งแล้ว" → ยืนยันด้วย SQL ว่า `shelved_status`/
  `shelved_by`/`shelved_at` ถูกเซ็ตถูกต้องจริง (แล้ว revert ค่ากลับเพราะเป็นแค่การทดสอบ ไม่ใช่งานจริง)

### 1c. ออเดอร์ยกเลิกหลังแพ็คแล้ว — เสี่ยงหลุดส่งไปกับขนส่ง (เพิ่มใหม่ 2026-08-31, ขยายความ 2026-09-01)
**แหล่งข้อมูล:** `https://www.bigseller.com/web/order/index.htm?status=In+Cancel` กรอง **"วันนี้"** เท่านั้น
เวลา sync จริง (ยืนยัน 2026-09-01 — "ถ้าจะซิงค์ตลอด ต้องเอาของวันนั้นๆ ไปดึง ถ้าดึงมาหมดจะงง") — การขยาย
เป็น 90 วันที่ทำระหว่างสำรวจ (หาตัวอย่างค่า "สถานะก่อนยกเลิก" ให้ครบ) เป็นแค่ขั้นตอนตรวจสอบครั้งเดียว
ไม่เกี่ยวกับ scope การ sync จริงที่จะรันซ้ำทุกวัน

**พบคอลัมน์สำคัญที่ใช้ตรวจจับได้ตรงๆ:** คอลัมน์ **"สถานะก่อนยกเลิก"** ในหน้านี้บอกว่าออเดอร์อยู่ใน
สถานะอะไรตอนที่ถูกยกเลิก

**ยืนยันแล้ว 2026-09-01 (ตรวจสอบค่าจริงจากหน้า BigSeller + ยืนยันเกณฑ์กับผู้ใช้) — เกณฑ์ปลอดภัย/เสี่ยง
อิงตามลำดับสถานะออเดอร์ของ BigSeller เอง (`คำสั่งซื้อใหม่ > กำลังจัดส่ง > รอรับสินค้า > จัดส่งแล้ว >
แพลตฟอร์มกำลังดำเนินการ > สำเร็จแล้ว`):**

| สถานะก่อนยกเลิก | ผลลัพธ์ |
|---|---|
| `ยังไม่ชำระ`, `คำสั่งซื้อใหม่`, `--` (ว่าง) | ปลอดภัย — ยังไม่ได้แพ็ค ไม่ต้องแจ้งเตือน |
| `กำลังจัดส่ง`, `รอรับสินค้า`, `จัดส่งแล้ว`, `แพลตฟอร์มกำลังดำเนินการ`, `สำเร็จแล้ว` | เสี่ยง — แพ็ค/ส่งไปแล้ว ต้องแจ้งเตือนทันทีและเข้า checklist ด้านล่าง |

เหตุผลที่ของถูกแพ็คพร้อมส่งไปแล้วก่อนจะรู้ว่าโดนยกเลิก คือเสี่ยงพนักงานส่งของไปทั้งที่ลูกค้ายกเลิกแล้ว

**แก้ไข 2026-09-01 (ตรวจสอบตอนเขียนโค้ดจริง) — ตารางด้านบนอิงจาก dropdown ตัวกรองในหน้าเว็บ ซึ่งเป็นคน
ละชุดค่ากับ field จริงที่ API ส่งมา** เจอ API จริงที่หน้านี้ใช้ (`POST /api/v1/order/new/pageList.json`,
`status: "canceled"`) แต่ละแถวมี `inCancelBeforeStatus` (โค้ดเครื่อง, ไม่ใช่ข้อความไทย) คู่กับ
`multilingualCancelBeforeStatus` (ข้อความไทยที่โชว์จริง) — สแกนตัวอย่างจริง 700 ออเดอร์ยกเลิก (90 วัน)
เจอค่าที่เป็นไปได้จริงครบตามนี้ (ต่างจากตารางด้านบนตรง "กำลังจัดส่ง" ไม่เคยเจอ พบ "กำลังดำเนินการ"/
"To Ship" แทนซึ่งเป็นแนวคิดเดียวกัน — order เข้าสู่กระบวนการเตรียมส่งแล้ว):

| `inCancelBeforeStatus` (โค้ด) | ข้อความไทย | จำนวนที่เจอ/700 | ผลลัพธ์ |
|---|---|---|---|
| `New`, `new` | คำสั่งซื้อใหม่ | 400 | ปลอดภัย |
| `unpaid` | ยังไม่ชำระ | 166 | ปลอดภัย |
| `To Ship` | กำลังดำเนินการ | 78 | เสี่ยง — เตรียมส่ง/แพ็คแล้ว |
| `To Pickup` | รอรับสินค้า | 13 | เสี่ยง — รอขนส่งมารับ พัสดุพร้อมแล้ว |
| `Shipped` | จัดส่งแล้ว | 2 | เสี่ยง — ส่งไปแล้วจริง |
| `null` (ไม่มีค่า) | — | 40 | **ไม่ทราบ → จัดเป็นเสี่ยง (default ปลอดภัยไว้ก่อนไม่ได้ ต้องให้คนเช็ค)** |
| `In Cancel` | คำขอยกเลิก | 1 | edge case หายาก → จัดเป็นเสี่ยงเช่นกัน |

**Implement เป็น allowlist ไม่ใช่ denylist:** โค้ดต้องเช็คว่า `inCancelBeforeStatus` อยู่ใน
`['New', 'new', 'unpaid']` ไหม ถ้าใช่ = ปลอดภัย ถ้าไม่ใช่ (รวมถึงค่าที่ไม่เคยเจอมาก่อน) = เสี่ยงเสมอ —
ปลอดภัยกว่าการ deny แค่ค่าที่รู้จัก เพราะถ้า BigSeller เพิ่มสถานะใหม่ในอนาคตที่เราไม่เคยเห็น ระบบจะแจ้ง
เตือนไว้ก่อน (false positive) แทนที่จะเงียบไปเฉยๆ (false negative ซึ่งอันตรายกว่ามาก)

**แก้ไข 2026-09-01 (ยืนยันจากผู้ใช้) — แจ้งเตือนอย่างเดียวไม่พอ ต้องเป็น checklist ให้พนักงานติดตาม
จนจบงานจริง** (แพทเทิร์นเดียวกับข้อ 1b — "ถูก flag แล้ว" ≠ "จบงานแล้ว"):

1. **เช็คว่าพัสดุถูกส่งออกไปกับขนส่งแล้วหรือยัง** — ถ้าทันดึงกลับก่อนขนส่งรับของ ถือว่าปลอดภัย ไม่ต้องทำ
   ขั้นต่อไป ถ้าส่งไปแล้วจริง ต้องประสานเรียกของคืนแยกต่างหาก (นอก scope ของระบบนี้ แค่ต้องรู้สถานะ)

**แก้ไขเพิ่ม 2026-09-01 (ยืนยันจากผู้ใช้ รอบ 2) — แยกขั้นที่ 2 เป็น 2 สถานะย่อยตามแพทเทิร์นเดียวกับ 1b
ไม่รวมเป็นค่าเดียว (`restockedStatus`) เหมือน draft แรก:**

2. **รับคืนหรือยัง** — ของที่ดึงกลับมาได้ทันถูกรับเข้าคลังจริงหรือยัง (แยกจาก "เก็บเข้าตำแหน่ง")
3. **เก็บแล้วหรือยัง** — หลังรับคืนแล้ว ถูกวางเก็บเข้าตำแหน่งจริงหรือยัง (ของที่รับคืนมาแต่ยังไม่เก็บเข้า
   ตำแหน่ง ยังนับเป็นงานค้างอยู่ดี ไม่ใช่จบเคสแค่รับคืนได้)

**ผู้รับผิดชอบ (ยืนยัน 2026-09-01):** งานข้อ 2-3 เป็นของ **ฝ่ายคลัง และทีมออฟไลน์ที่ดูแลรับคืนสินค้าเข้า
ระบบ** (ไม่ใช่ฝ่ายคลังอย่างเดียวเหมือนข้อ 1b) — dropdown เลือกชื่อผู้ทำงาน (ดู addendum "การอ้างอิงตัว
ผู้ทำงาน") ต้องกรองให้เลือกได้ทั้ง 2 กลุ่มนี้ ไม่ใช่กรองเฉพาะ `department = 'ฝ่ายคลัง'` แบบ 1b

**ต้องสำรวจก่อนเขียนโค้ด (ห้ามเดา):** BigSeller มีสถานะ/หน้าไหนที่บอก 2 ขั้นนี้ตรงๆ ไหม (เช่น สถานะ
"ยกเลิกแล้ว รอดึงกลับ" vs "ดึงกลับสำเร็จ" vs "Stock-In แล้ว") หรือต้องอนุมานจากหลายหน้าประกอบกัน
(เช่น เทียบกับหน้า "การคืนสินค้า Stock-In" ที่ใช้ใน `FEATURE-return-exception-audit.md` อยู่แล้ว — ถ้า
ออเดอร์เดียวกันโผล่ในนั้นด้วยแปลว่าเข้าสู่กระบวนการคืนแล้ว, และเช็คเมนู "การจัดการหลังการขาย" ที่เห็นใน
sidebar หน้าออเดอร์ ว่ามีหน้ารับคืน/stock-in ที่ตรงกับ 2 สถานะนี้อยู่แล้วหรือไม่)

ดึงเข้า `DB_TRANSFER_EXCEPTIONS` หรือแยกตารางใหม่ `DB_CANCELLED_AFTER_PACK` (คอลัมน์: `orderNo,
cancelTime, statusBeforeCancel, cancelReason, shippedOutStatus, shippedOutBy, shippedOutAt,
receivedBackStatus, receivedBackBy, receivedBackAt, shelvedStatus, shelvedBy, shelvedAt,
sourceUrl`) — `*By`/`*At` คือพนักงานที่กดเช็คในระบบเราเอง (ดู addendum ด้านล่าง) — sync เฉพาะแถวที่
`statusBeforeCancel` เข้าเงื่อนไข "แพ็คแล้ว" เท่านั้น ไม่ต้องเก็บทุกแถวของออเดอร์ยกเลิกทั้งหมด (มีถึง
42,234 รายการ เก็บเฉพาะที่เข้าเงื่อนไขเสี่ยงจริงพอ)

**สร้างเสร็จแล้ว 2026-09-01 — ฝั่ง sync/backend ของ 1c พร้อมใช้งานจริง:**
- ตาราง Supabase `cancelled_after_pack` สร้างแล้ว (คอลัมน์ตรงตามด้านบน แบบ snake_case, แยก
  `received_back_status/by/at` กับ `shelved_status/by/at` ตามที่ยืนยันไว้)
- ข้อมูลดึงผ่าน API เดียวกับที่ใช้ enumerate สถานะด้านบน (`POST /api/v1/order/new/pageList.json`,
  `status: "canceled"`) ดู `src/bigseller/cancelled-orders-page.ts`
- **ข้อจำกัดที่ยอมรับไว้ (ไม่ได้เดาเงียบๆ):** ไม่สามารถหา `timeType` ที่กรองด้วย "เวลาขอยกเลิก" ได้จริง
  (ลองผ่าน UI หลายรอบ คลิก dropdown ไม่สำเร็จ และการยิง API ถี่ๆ เพื่อทดสอบค่า `timeType` ต่างๆ ไปโดน
  rate limit ของ BigSeller จน session หลุดเอง) — แก้ด้วยการดึงช่วงกว้างกว่าจริง (`days=7` อิง "เวลาสั่งซื้อ"
  ซึ่งเป็นค่าเริ่มต้นที่ยืนยันได้จริง) แล้วกรองซ้ำฝั่งเราเองด้วย `cancelTimeStr` ของแต่ละแถวตรงๆ (parse
  ด้วย `src/utils/thai-date.ts` ที่เขียนใหม่) — วิธีนี้แม่นยำกว่าพึ่ง quick-filter ของ BigSeller เอง แต่มี
  ช่องโหว่ทางทฤษฎี: ออเดอร์ที่ค้างมากกว่า 7 วันตั้งแต่สั่งซื้อ (ยังไม่แพ็ค/ส่ง) แล้วเพิ่งถูกยกเลิกวันนี้จะ
  หลุดสแกน — ยอมรับ trade-off นี้เพราะออเดอร์ที่เข้าเงื่อนไข "แพ็คแล้ว/ส่งแล้ว" ตามธรรมชาติควรเกิดขึ้น
  ภายในไม่กี่วันหลังสั่งซื้ออยู่แล้ว ไม่ใช่ค้างเป็นสัปดาห์
- `mark_cancelled_after_pack_step(order_no, staff_id, step)` — Postgres RPC เดียวครอบคลุมทั้ง 3 ขั้น
  (`shipped_out`/`received_back`/`shelved`) แทนที่จะแยก 3 ฟังก์ชัน ตรวจสอบ staff id ก่อนทุกครั้งเหมือน 1b
- โค้ด: `src/bigseller/cancelled-orders-page.ts`, `src/services/cancelled-after-pack-service.ts`,
  `scripts/sync-cancelled-after-pack.ts` (`npm run sync:cancelled-after-pack`) — standalone script
  เหมือน 1b ยังไม่ผูกเข้า `syncBigSeller()` อัตโนมัติ
- **ทดสอบจริงแล้ว (2026-09-01):** พบบั๊กจริงระหว่างทดสอบ — โค้ดฉบับแรกหยุด scan ที่หน้า 10 (สมมติว่า
  ปลอดภัยเมื่อเจอ 0 รายการติดกันหลายหน้า) แต่ log จริงแสดงว่าหน้า 7 เจอ 0 แต่หน้า 8 เจออีก 2 รายการ —
  แก้เป็น scan ให้ครบทั้ง 7 วันเสมอ (32 หน้า, ~60 วินาที, ไม่โดน rate limit เพราะเว้น 1 วินาทีต่อหน้า)
  ผลหลังแก้: เจอ 239 ออเดอร์ยกเลิกวันนี้ เข้าเงื่อนไขเสี่ยง 60 รายการ (ก่อนแก้เจอแค่ 56 — ยืนยันว่าบั๊ก
  ทำให้พลาดข้อมูลจริง) เขียนเข้า Supabase สำเร็จ พบเคสจริงที่ "จัดส่งแล้ว" ก่อนโดนยกเลิก
  (`BSUJR3N41505`) ตรงตามความเสี่ยงที่ฟีเจอร์นี้ตั้งใจจับ
- **สร้างเสร็จแล้วและทดสอบจริงแล้ว 2026-09-01 — UI checklist ใน GV Ops Console:** section ที่ 2 ของ
  `src/pages/Checklist.jsx` — dropdown กรอง `department = 'ฝ่ายคลัง' OR handles_returns` ตามที่ยืนยันไว้
  ปุ่มแยก 3 ขั้น (ส่งออกไปแล้ว/รับคืนแล้ว/เก็บเข้าตำแหน่งแล้ว) เรียก RPC
  `mark_cancelled_after_pack_step` ทดสอบจริงผ่าน Playwright แล้ว (เลือกชื่อ "fern" กด "ส่งออกไปแล้ว" →
  ยืนยันด้วย SQL ว่าเซ็ตถูกต้อง แล้ว revert เพราะเป็นแค่การทดสอบ)

### 1d. ใบเติมสต็อกชั้นวาง / ย้ายสินค้าภายในคลัง ค้างนาน — งานประจำวันปกติของฝ่ายคลัง (เพิ่มใหม่ 2026-09-01)

**ต่างจาก 1b/1c ตรงที่นี่คืองานประจำวัน (routine) ไม่ใช่ exception** — แต่ยังมีความเสี่ยง "ค้างนานผิดปกติ"
ที่ควรเข้าไปดูเหมือนกัน จึงจัดเป็นรายการย่อยในกลุ่ม "ต้องดูก่อน" (หัวข้อ 1) ได้เมื่อค้างเกิน threshold

**แหล่งข้อมูล (ยืนยันจากผู้ใช้ 2026-09-01, มาพร้อมภาพหน้าจอจริง):**
- `https://www.bigseller.com/web/inventory/replenishment/replenishmentShelves.htm` — ใบ "เติมสต็อก
  ชั้นวาง" (เติมของขึ้นชั้นหยิบ)
- `https://www.bigseller.com/web/inventory/movingGoods/index.htm` — ใบ "ย้ายสินค้า" ภายในคลังเดียวกัน
  (**คนละอย่างกับ "โอนสินค้า" ข้ามคลังในข้อ 1b** — อันนี้ย้ายตำแหน่งภายในคลังเดียวกัน ไม่ใช่ข้ามคลัง)

**Workflow จริง (อธิบายจากผู้ใช้):** ฝ่ายรับเข้าหยิบสินค้าจากตำแหน่งเก็บ (bulk storage) ใส่ลิฟต์ขึ้นไปชั้น
3/ชั้น 5 (ชั้นหยิบ) ให้ฝ่ายคลังเช็คตามใบเติมสต็อกว่าครบ แล้วเก็บเข้าตำแหน่งหยิบ (pick location) จริง

**สถานะที่ BigSeller เก็บเองอยู่แล้ว (ใช้ตรงๆ ไม่ต้องคำนวณเอง)** — ทั้ง 2 หน้ามีชุดแท็บเดียวกัน: รอนำเข้า
→ รอหยิบสินค้า → กำลังหยิบสินค้า → กำลังนำเข้าชั้นวาง → เสร็จเรียบร้อย (มี "ยกเลิก" แยกด้วย)

**จุดสำคัญ — ต่างจาก 1b/1c เรื่อง staff attribution:** ทั้ง 2 หน้านี้ BigSeller เก็บคอลัมน์ "ผู้สร้าง" และ
"Operator" (ผู้ดำเนินการจริง) ไว้ในตัวเองอยู่แล้ว (เช่น "เตอร์ St_ter@UJR3128999") — **ไม่ต้องสร้าง staff
dropdown เองสำหรับงานนี้** อ่านค่า Operator จาก BigSeller ตรงๆ ได้เลย ต่างจาก 1b/1c ที่ BigSeller ไม่มี
ผู้ทำงานให้ (เลยต้องใช้ dropdown เราเอง)

**คำถามที่ต้องถามก่อนเขียนโค้ด (ห้ามเดา):**
- เป้าประจำวันของฝ่ายคลังสำหรับงานนี้ควรวัดเป็นอะไร (จำนวนใบเติมสต็อกที่ทำเสร็จ/วัน? จำนวน SKU/ชิ้นที่
  เติม? นับต่อคนหรือรวมทั้งฝ่าย?) และมีตัวเลขเป้าที่ใช้จริงอยู่แล้วไหม (แบบ 350/คน/วันของฝ่ายออนไลน์-หยิบ)
  หรือให้เริ่มจากแค่โชว์ backlog/สถานะค้างก่อน ยังไม่ต้องมีเป้าตายตัว
- threshold ชั่วโมงที่ถือว่า "ค้างนานผิดปกติ" สำหรับใบเติมสต็อก/ย้ายสินค้า (เหมือนที่ 1b ถามเรื่อง 24 ชม.)

ดึงเข้าตารางใหม่ `DB_REPLENISHMENT_BACKLOG` (คอลัมน์: `slipNo, warehouse, skuCount, docType
[เติมสต็อก/ย้ายสินค้า], creator, operator, status, createdAt, updatedAt, hoursInStatus`) —
`creator`/`operator` ดึงจาก BigSeller ตรงๆ ไม่ต้องมีคอลัมน์ `*By` แบบ 1b/1c

**เชื่อมกับหัวข้อ 2 (การ์ดเป้าประจำวัน):** นี่คือแหล่งข้อมูลจริงตัวแรกที่เติมการ์ด "ฝ่ายคลัง" ที่เดิมยังเป็น
placeholder เทาๆ ได้ — แต่ต้องตอบคำถามเป้าหมายด้านบนก่อนถึงจะออกแบบการ์ดสี (เขียว/เหลือง/แดง) ได้ ถ้ายัง
ไม่มีเป้า ให้โชว์แค่ตัวเลข backlog ดิบไปก่อน (จำนวนใบที่ยังไม่เสร็จ + อายุงานเก่าสุด) แบบเดียวกับหัวข้อ 3

**สร้างเสร็จแล้ว 2026-09-01 — ฝั่ง sync/backend ของ 1d พร้อมใช้งานจริง:**
- ตาราง Supabase `warehouse_backlog` สร้างแล้ว (`slip_no, warehouse, doc_type ['replenishment'/'moving'],
  sku_count, status_code, creator, operator, bigseller_created_at, bigseller_updated_at, source_url`)
  — **replaceAll ทุกรอบ ไม่ใช่ upsert** (ต่างจาก 1b/1c) เพราะไม่มี manual overlay ต้องรักษา ทุกคอลัมน์มา
  จาก BigSeller ล้วนๆ และ sync เฉพาะแถวที่ยังไม่จบงาน (non-terminal) อยู่แล้ว พอใบไหนเสร็จก็หายจากตาราง
  นี้เองในรอบถัดไป (แบบเดียวกับ `DB_PENDING_ORDER_DEMAND`)
- พบ API จริง 2 ตัว: `wms/repletePlan/pageList.json` (เติมสต็อกชั้นวาง, ใช้ numeric `repleteState` โค้ด
  จาก `getRepletePlanStateCount.json`) และ `movingPlan/pageList.json` (ย้ายสินค้า) — **API นับสถานะของ
  2 หน้านี้คนละรูปแบบกัน:** repletePlan ใช้ numeric key ตรงๆ (`{"0":6,"2":9149,"3":27987}`) แต่
  movingPlan ใช้ named key (`{"notStarted":0,"picking":0,"storing":0,"finish":1575,"canceled":7,...}`)
  ที่ map กลับเป็นโค้ด numeric ที่ `pageList.json` ต้องการไม่ได้ — แก้โดยดึง movingPlan แบบไม่ระบุ `state`
  (ได้ทุกสถานะ) ในช่วง 2 วันล่าสุดแทน แล้วกรอง client-side ด้วยโค้ด terminal ที่ยืนยันแล้ว (2, 3)
- **ยืนยัน state code ที่แน่นอน:** `0` = รอนำเข้า (ยืนยันจาก request ตอนเปิดหน้าเข้าแท็บ default),
  `2` = เสร็จเรียบร้อย, `3` = ยกเลิก (ยืนยันจากยอดรวม unfiltered query ตรงกับผลรวม state 2+3 พอดีเป๊ะทั้ง
  2 API) — ใช้ pattern เดียวกันทั้งคู่ (backend เดียวกัน) `1/4/5` ที่เหลือ**ไม่ทราบชื่อ stage ที่แน่ชัด**
  แต่ไม่กระทบการใช้งาน เพราะจัดเป็น "ยังไม่เสร็จ = backlog" รวมกันหมด ไม่ต้องแยกราย stage ก็พอสำหรับ
  ตอนนี้ — ยืนยันจริงแล้วว่า status_code=4 มีข้อมูลจริง (เจอระหว่างทดสอบ พร้อม operator "ตั้น")
  ผู้อ่านโค้ดในอนาคตที่อยากรู้ชื่อ stage 1/4/5 ที่แน่ชัด ต้องสำรวจเพิ่มตอนมีข้อมูลในสถานะนั้นจริง (ห้ามเดา)
- ไม่มี staff dropdown/RPC เหมือน 1b/1c — BigSeller มี `operator`/`creator` ให้ในตัวเองแล้ว
- โค้ด: `src/bigseller/warehouse-backlog-page.ts`, `src/services/warehouse-backlog-service.ts`,
  `scripts/sync-warehouse-backlog.ts` (`npm run sync:warehouse-backlog`), และ `src/utils/thai-date.ts`
  (parser ใหม่ ใช้ร่วมกับ 1c ด้วย) — standalone script เหมือน 1b/1c ยังไม่ผูกเข้า `syncBigSeller()`
- **ทดสอบจริงแล้ว (2026-09-01):** เจอ backlog จริง 6 ใบ (ทั้งหมดเป็น `replenishment`, `moving` ตอนนี้
  ว่างสนิท) เขียนเข้า Supabase สำเร็จ
- **ยังไม่ได้ตอบ:** คำถามเป้าประจำวัน/threshold ค้างนานผิดปกติ (ยังไม่มีใครตอบ) — ไม่ block backend
  แต่ block การออกแบบการ์ดสี/alert ในหน้า home จริง



### 2. การ์ดเป้าประจำวัน — 1 การ์ดต่อฝ่าย
```
┌─────────────────────┐
│ ฝ่ายออนไลน์ - หยิบ    │
│ 214 / 300 พัสดุ (71%) │
│ ▓▓▓▓▓▓▓░░░  ต่ำกว่าที่ควร ณ ตอนนี้ │
│ เมื่อวาน: 298 พัสดุ    │
└─────────────────────┘
```
- แสดงเฉพาะฝ่ายที่มีข้อมูลจริงแล้ว (ตอนแรกมีแค่ "ฝ่ายออนไลน์-หยิบ")
- **ฝ่ายที่ยังไม่มีข้อมูล (ออฟไลน์, รับเข้า, แพ็ค) ให้โชว์การ์ดเทาๆ "ยังไม่เชื่อมข้อมูล"** แทนการซ่อนไป
  เลย — เผื่อไว้ให้ต่อ layout เดิมได้ทันทีเมื่อ Step ในอนาคตมีข้อมูลแล้ว ไม่ต้องออกแบบใหม่
- สีการ์ด: เขียว (≥100% เทียบเป้าที่ปรับตามเวลา), เหลือง (70-99%), แดง (<70%) — เกณฑ์นี้ปรับได้
  ทีหลังตามที่หัวหน้าฝ่ายใช้จริงแล้วเห็นว่าเหมาะสมไหม ไม่ต้อง fix เป็นค่าตายตัวถาวร
- **ทุกการ์ดต้องมีบรรทัด "เมื่อวาน" ประกอบเสมอ** (ยืนยันจากผู้ใช้ 2026-08-31) — ให้เทียบได้ทันทีว่าวันนี้
  กำลังดีขึ้นหรือแย่ลงกว่าเมื่อวาน ไม่ใช่ดูแค่เทียบเป้าอย่างเดียว

### 2b. การ์ดยอดขาย/ยอดพัสดุ/จำนวนสินค้า — วันนี้ vs เมื่อวาน ต่อฝ่าย (เพิ่มใหม่ 2026-08-31)
นอกจากการ์ดเป้าหยิบ/แพ็คแล้ว หน้า home ต้องมีการ์ดสรุป **3 ตัวชี้วัดนี้แยกต่อฝ่าย** เทียบวันนี้กับ
เมื่อวานเสมอ (ไม่ใช่แค่ตัวเลขวันนี้ลอยๆ):
- **ยอดขาย** (บาท) — จาก `DB_SALES_SUMMARY` (`FEATURE-sales-online-offline-report.md`) แยก
  online/offline ตามฝ่าย
- **ยอดพัสดุ** (จำนวนพัสดุที่จัดการแล้ว) — จาก `DB_OPERATOR_PERFORMANCE` หรือแหล่งที่เกี่ยวข้องของ
  แต่ละฝ่าย
- **จำนวนสินค้า** (SKU/ชิ้นที่ขาย) — จาก `DB_SALES_SUMMARY` เช่นกัน (มีคอลัมน์ `productSalesCount`
  อยู่แล้วในสเปกนั้น)

รูปแบบการ์ด: ตัวเลขวันนี้ตัวใหญ่ + ตัวเลขเมื่อวานตัวเล็กข้างล่างพร้อม % เปลี่ยนแปลง (แบบเดียวกับ
"Total Budget" card ในภาพตัวอย่าง Excel dashboard ที่เคยดูสไตล์กัน คือมีลูกศรขึ้น/ลงบอกทิศทาง)

**ฝ่ายที่ยังไม่มีข้อมูลยอดขาย/พัสดุแยกฝ่ายได้ (เช่น ฝ่ายคลัง/รับเข้าที่ไม่ได้ผูกกับยอดขายตรงๆ) ให้ข้าม
การ์ดชุดนี้ไปแทนที่จะโชว์ค่า 0 หรือค่าว่าง** — ตัวชี้วัดนี้เหมาะกับฝ่ายที่งานผูกกับยอดขาย/พัสดุโดยตรง
(ออนไลน์, ออฟไลน์) มากกว่าฝ่ายสนับสนุน (คลัง, รับเข้า) ที่ควรใช้ตัวชี้วัดของตัวเองแทน (ดูหัวข้อ 2)

### 3. รายการงานค้างแยกตามฝ่าย (ใต้การ์ดเป้า)
ตารางง่ายๆ ต่อฝ่าย: จำนวนงานค้าง, อายุงานค้างเก่าสุด — ให้เห็นว่าฝ่ายไหนงานกองสะสมนานแค่ไหน ไม่ใช่
แค่จำนวนรวม (10 งานค้างที่เพิ่งเกิดเมื่อเช้า ≠ 10 งานค้างที่ค้างมา 3 วันแล้ว)

## เป้าที่ปรับตามเวลา (สำคัญ — อย่าทำเป้าคงที่ทั้งวัน)
เป้า 300 พัสดุ/วัน ถ้าโชว์ "214/300 (71%)" ตอน 10 โมงเช้า จะดูเหมือนพนักงานทำงานช้า ทั้งที่จริงยังไม่
ถึงเที่ยงด้วยซ้ำ — ต้องมีตัวเทียบที่สองเสมอ: **"ควรได้ประมาณเท่าไหร่ ณ เวลานี้ของวัน"** โดยอิงจากอัตรา
เฉลี่ยของสัปดาห์ที่ผ่านมาในช่วงเวลาเดียวกัน (ไม่ใช่หารเป้าเป็นเส้นตรงตามเวลาเปิด-ปิดกะ เพราะงานจริง
มักไม่กระจายเท่าๆ กันตลอดวัน) — **ต้องคุยกับหัวหน้าฝ่ายก่อนว่าจะใช้สูตรไหน อย่าเดาเอง** เพราะเป็น
business rule ที่กระทบการตัดสินใจจริง (จะเข้าไปเตือนพนักงานหรือไม่)

## ข้อมูลที่ต้องใช้ (จาก Supabase ตามที่ตั้งไว้ใน FEATURE-web-dashboard-supabase.md)
- `operator_performance` — เป้า vs จริง
- `DB_TRANSFER_EXCEPTIONS` (ถ้าย้ายมา Supabase แล้ว, ถ้ายังไม่ย้าย ต้องอ่านจาก Sheets แยก — เช็คว่า
  Step 4 ทำครบหรือยัง)
- ตาราง "ของขาด" order status (ส่วนเสริมที่ระบุใน addendum ของ Step 1)
- decoy reconciliation exceptions

## ห้ามทำ
- ห้ามออกแบบให้หน้า home โชว์ตารางข้อมูลดิบเป็นหลัก — ทุก widget ต้องผ่านการสรุป/จัดลำดับมาก่อนแล้ว
- ห้ามเดาสูตร "เป้าที่ปรับตามเวลา" เอง — ต้องคุยกับผู้ใช้ก่อนว่าจะใช้เกณฑ์ไหน
- ห้ามซ่อนฝ่ายที่ยังไม่มีข้อมูล — ให้โชว์เป็นการ์ด placeholder แทน

## คำถามที่ต้องถามก่อนเขียนจริง
- เกณฑ์สี (แดง/เหลือง/เขียว) ที่ 70%/100% เหมาะไหม หรือหัวหน้าฝ่ายมีเกณฑ์อื่นที่ใช้อยู่แล้ว
- สูตร "เป้าที่ปรับตามเวลา" อยากใช้แบบไหน (เส้นตรงตามเวลากะ / อิงค่าเฉลี่ยย้อนหลัง / อื่นๆ)

## UI reference (2026-08-31) — widget type ต่อส่วน จากตัวอย่างที่ผู้ใช้ส่งมา

| ส่วนในหน้า home | widget type ที่ใช้ | เหตุผล |
|---|---|---|
| การ์ดเป้าประจำวันต่อฝ่าย (หัวข้อ 2) | แท่ง bar เดี่ยว + ตัวเลข "จริง / เป้า" คู่กัน (ไม่ใช่แค่ progress bar percentage เฉยๆ) | เห็นทั้งค่าจริงและเป้าในสายตาเดียว อ่านไวกว่าเปอร์เซ็นต์ล้วน |
| รายการงานค้างแยกตามฝ่าย (หัวข้อ 3) | horizontal bar list เทียบสัดส่วน แทนตารางตัวเลขเรียงคอลัมน์ | อ่านเปรียบเทียบระหว่างฝ่ายได้เร็วกว่าตัวเลขล้วน |
| แถบ "ต้องดูก่อน" (หัวข้อ 1) — สรุปสัดส่วนรวม | pie/donut เล็กๆ ประกอบ (resolve แล้ว vs ค้างอยู่) ควบคู่กับลิสต์รายการ ไม่ใช่แทนลิสต์ | ให้เห็น "หนักแค่ไหน" ก่อนไล่อ่านทีละบรรทัด |
| งานค้างที่มี trend รายวัน (ของใหม่ ยังไม่มีในสเปกเดิม) | stacked bar แบบ burndown chart (completed/pending/added ต่อวัน ลดหลั่น) | เห็นว่างานค้างกำลังดีขึ้นหรือแย่ลง ไม่ใช่แค่ snapshot ปัจจุบัน — ควรเพิ่มเป็น section ใหม่ใต้หัวข้อ 3 |
| ภาพรวมทั้งบริษัท (ถ้าทำแยกจากภาพรวมต่อฝ่าย) | ตัวเลขใหญ่ + donut สัดส่วนเล็กประกอบ | ใช้กับผู้บริหารที่ดูภาพรวมข้ามฝ่าย ไม่ใช่หัวหน้าฝ่ายเดี่ยว |

**ธีมสี:** หน้า home/ภาพรวมใช้ธีมมืด (สอดคล้องกับหน้า ranking ของ BigSeller เองที่เคยดูเป็นตัวอย่าง)
หน้า drill-down (ตาราง/รายละเอียด) ใช้ธีมสว่าง อ่านนานได้สบายตากว่า — ปรับได้ภายหลังถ้าหัวหน้าฝ่าย
ลองใช้แล้วไม่ถูกใจ ไม่ใช่ข้อกำหนดตายตัว

**Radar chart (จากตัวอย่าง "Sources"):** ยังไม่มีจุดที่เข้ากับหน้า home ชัดเจน — เก็บไว้พิจารณาสำหรับ
หน้าโปรไฟล์ operator รายคนในอนาคต (เทียบหลายมิติ เช่น ความเร็ว/ความแม่นยำ/ความสม่ำเสมอ) ไม่ใช่ scope
ปัจจุบัน

## Addendum (2026-08-31): เป้าฝ่ายออนไลน์-หยิบ แก้เป็น 350/คน/วัน
ตัวเลข 300 พัสดุ/วัน ที่ใช้เป็นตัวอย่างประกอบเอกสารนี้ (การ์ดตัวอย่าง "214/300") เป็นค่าคาดเดา — ค่าจริง
จากไฟล์ `รายงานของทีม.xlsx` คือ **350/คน/วัน** (ดูรายละเอียดใน `FEATURE-operator-picking-performance.md`
หัวข้อ 3) ตัวอย่างตัวเลขในเอกสารนี้ยังคงไว้เพื่ออธิบายแนวคิด layout ไม่ต้องแก้ย้อนหลัง แต่ตอน implement
จริงให้ใช้ 350 เป็นค่าเป้า ไม่ใช่ 300

## Addendum (2026-08-31): ระบบแรงกิ้ง (ranking) — เพิ่มใหม่ ไม่มีใน draft แรกของไฟล์นี้
เพิ่ม section ใหม่ **"อันดับผลงานวันนี้ (แยกตามฝ่าย)"** ในหน้า home ต่อจากหัวข้อ 2b (การ์ดยอดขาย/พัสดุ/
สินค้า) ก่อนหัวข้อ 3 (งานค้างแยกตามฝ่าย) — จุดประสงค์: สร้างแรงกระตุ้นเชิงบวก ไม่ใช่เครื่องมือเปรียบเทียบ
ลงโทษ

**กติกาที่ยืนยันแล้ว (2026-08-31):**
- **จัดอันดับจากจำนวนพัสดุ/ชิ้นที่หยิบ-แพ็คอย่างเดียว** ไม่ผสมตัวชี้วัดความแม่นยำ/exception เข้าไปใน
  คะแนนอันดับ (แหล่งข้อมูล: ดู addendum ด้านล่าง — เปลี่ยนจาก `DB_OPERATOR_PERFORMANCE` เป็น
  `operator_wave_ranking` แล้ว)
- **แยกลิสต์ต่อฝ่าย ไม่รวมเป็นอันดับเดียวข้ามฝ่าย** — เพราะหน่วยวัดของแต่ละฝ่ายไม่เท่ากัน (พัสดุ vs
  ยอดขาย vs รายการรับเข้า) เทียบกันตรงๆ ไม่ยุติธรรม แสดงเป็นการ์ดแยกต่อฝ่าย (Top 3-5 ต่อการ์ด) ฝ่ายที่
  ยังไม่มีข้อมูลใช้ placeholder เทาแบบเดียวกับการ์ดเป้า (หัวข้อ 2)
- **คนที่ต่ำกว่าเป้าไม่โชว์ในลิสต์อันดับ** — ลิสต์นี้เป็นด้านบวกล้วนๆ (โชว์เฉพาะ Top ผลงาน) คนเป้าต่ำจะไป
  ปรากฏที่แถบ "ต้องดูก่อน" (หัวข้อ 1) แทน ผ่านเงื่อนไข operator ต่ำกว่า 80% ของเป้าปรับเวลาที่มีอยู่แล้ว

**แก้ไข 2026-09-01 — มีข้อมูลจริงครบทั้งฝ่ายออนไลน์และฝ่ายออฟไลน์แล้ว ไม่ใช่แค่ฝ่ายออนไลน์-หยิบอีกต่อไป:**
พบหน้า "แผงรายงาน Wave" (`waveWorkBoard.htm`) ที่ให้ทั้งอันดับหยิบ (`pickRank`) และอันดับบรรจุ
(`packageRank`) แบบเรียลไทม์ ครอบคลุม operator ทุกคน (ทั้งออนไลน์/ออฟไลน์/คลัง) ในเรียกเดียว — สร้างตาราง
`operator_wave_ranking` และ sync จริงแล้ว (`npm run sync:operator-wave-ranking`, ดูรายละเอียดใน
`FEATURE-operator-picking-performance.md`) ทดสอบแล้ว join กับ `staff` ได้ครบทุกคน (41 แถวจริง) — การ์ด
ranking ในหน้า home ตอนนี้มีข้อมูลจริงพร้อมใช้ได้มากกว่าแค่ฝ่ายออนไลน์-หยิบ: ฝ่ายออนไลน์ (หยิบ+บรรจุ) และ
ฝ่ายออฟไลน์ (บรรจุ) มีข้อมูลจริงแล้ว ส่วนฝ่ายคลัง/รับเข้ายังต้องต่อกับ 1d (`warehouse_backlog`) แยกต่างหาก
เพราะงานคนละประเภทกัน (ไม่ใช่หยิบ/บรรจุพัสดุลูกค้า)

**ต่อกับหน้า drill-down:** `FEATURE-department-pages.md` มี ranking แบบเดียวกันแต่กรองตามช่วงวันที่ที่
เลือกได้ (ไม่ใช่แค่วันนี้) — ดูรายละเอียดที่ไฟล์นั้น

## Addendum (2026-09-01): การอ้างอิงตัวผู้ทำงาน (staff attribution) สำหรับ checklist 2 ขั้น (1b, 1c)

เดิมพิจารณาจะดึงชื่อ "คนโอน/คนรับ" จากหน้า BigSeller เอง (`หมายเลขการโอน` → `รายละเอียด`) แต่ผู้ใช้
ยืนยันว่า **ไม่ต้องดึงจาก BigSeller** — แต่ละขั้นของ checklist (กดรับ/เก็บเข้าตำแหน่ง/ส่งออกแล้ว/
ดึงกลับเข้าคลัง) ให้ผูกกับ "ใครกดเช็คในระบบ GV Ops Console เอง" แทน เพื่อใช้เป็น accountability
trail ของงานจริงที่ทำในระบบเรา ไม่ใช่ mirror ข้อมูลจาก BigSeller

**กลไกที่ยืนยันแล้ว (2026-09-01): เลือกชื่อจาก dropdown ไม่มี login/password**
- เหตุผล: หน้างานคลัง/หยิบ-แพ็คต้องการความเร็ว ไม่เหมาะกับการ login ทุกครั้งที่กดเช็ค
- **สร้างเสร็จแล้ว 2026-09-01:** ตาราง `staff` ใน Supabase project `gv-ops-console`
  (`id, name, department, role_note, handles_returns, active, created_at`) seed ด้วยรายชื่อจริง
  30 คนจากผู้ใช้แล้ว (`0@UJR317111`, `atom@UJR3010721`, ... ครบทุกฝ่าย) — dropdown ดึงรายชื่อจาก
  ตารางนี้ ไม่ hardcode ชื่อในโค้ดฝั่ง frontend เพราะพนักงานเข้า-ออกเปลี่ยนบ่อย (เพิ่ม/ลบคนแค่แก้ตาราง)
- ทุกครั้งที่กดเช็ค 1 ขั้น ให้บันทึก `{action}By` (staff.id ที่เลือก) และ `{action}At` (timestamp)
  คู่กันเสมอ — ใช้กับทุกขั้นที่เป็น **manual checklist ผ่านหน้าเราเท่านั้น**: `DB_TRANSFER_IN_TRANSIT`
  (`shelvedBy/shelvedAt` — ส่วน `receivedStatus` อ่านจาก BigSeller ตรงๆ ไม่มี `receivedBy`) และ
  `DB_CANCELLED_AFTER_PACK` (`shippedOutBy/shippedOutAt`, `receivedBackBy/receivedBackAt`,
  `shelvedBy/shelvedAt`)
- **จุดอ่อนที่ต้องยอมรับไว้ก่อน:** dropdown ไม่มีการยืนยันตัวตนจริง (ใครก็เลือกชื่อคนอื่นแทนได้) — ยอมรับ
  trade-off นี้เพื่อความเร็วหน้างาน ถ้าในอนาคตพบปัญหาเลือกชื่อผิด/แอบอ้าง ค่อยพิจารณา PIN สั้นๆ เพิ่ม
  ทีหลัง ไม่ใช่ทำ full auth ตั้งแต่แรก
- ตาราง `staff` นี้เป็นโครงสร้างพื้นฐานร่วม — ใช้ซ้ำได้กับฟีเจอร์ในอนาคตที่ต้องรู้ตัวผู้ทำงานเป็นรายคน
  (เช่น ranking รายคนแทนที่จะเป็นแค่ระดับฝ่าย ถ้าต้องการในอนาคต)

**ยืนยันเพิ่ม (2026-09-01): checklist 1b (รับ-เก็บใบโอน) เป็นงานของ "ฝ่ายคลัง" ล้วนๆ** (ฝ่ายที่ merge
กับฝ่ายรับเข้าแล้วตาม addendum ก่อนหน้านี้) — ผลกระทบต่อการ implement:
- Dropdown เลือกชื่อผู้ทำงานใน checklist 1b **ให้กรองเฉพาะ `staff.department = 'ฝ่ายคลัง'`** ไม่ต้อง
  โชว์รายชื่อพนักงานฝ่ายอื่นปนมาให้เลือกผิดง่าย
- ในหน้า home หัวข้อ 2 (การ์ดเป้าประจำวัน) และหัวข้อ 3 (งานค้างแยกตามฝ่าย) งานจาก 1b ให้นับรวมอยู่ใต้
  การ์ด/แถวของ "ฝ่ายคลัง"

**แก้ไข 2026-09-01 (รอบ 2): checklist 1c (ยกเลิกหลังแพ็ค → รับคืน-เก็บเข้าตำแหน่ง) เป็นงานร่วมของ
"ฝ่ายคลัง" และ "ทีมออฟไลน์ที่ดูแลรับคืนสินค้าเข้าระบบ" — ไม่ใช่ฝ่ายคลังอย่างเดียวเหมือน 1b:**
- **ยืนยันแล้ว 2026-09-01 (รอบ 3) — ได้รายชื่อจริงจากผู้ใช้ สร้างตาราง `staff` ใน Supabase
  (`gv-ops-console` project) พร้อม seed ข้อมูลจริง 30 คนแล้ว** คอลัมน์: `id` (BigSeller operator
  login เช่น `fern@UJR3010722`), `name`, `department` (`ฝ่ายคลัง`/`ฝ่ายออนไลน์`/`ฝ่ายออฟไลน์`/`แอดมิน`),
  `role_note`, `handles_returns` (boolean), `active`
- **ทีมออฟไลน์ที่ดูแลรับคืนสินค้าโดยเฉพาะ (ไม่ใช่ทั้งฝ่ายออฟไลน์) คือ `fern@UJR3010722` และ
  `st_punch@UJR3063224`** (บันทึกไว้เป็น `department = 'ฝ่ายออฟไลน์'` + `handles_returns = true` ใน
  ตาราง `staff`) — คนอื่นในฝ่ายออฟไลน์มีหน้าที่หลักคือ "จัดออเดอร์" ไม่เกี่ยวกับรับคืน
- Dropdown เลือกชื่อผู้ทำงานใน checklist 1c **ให้กรอง `staff.department = 'ฝ่ายคลัง' OR
  staff.handles_returns = true`** (ไม่ใช่ทั้งฝ่ายออฟไลน์แบบเดา — ใช้ flag ที่ยืนยันแล้วแทน)
- ในหน้า home งานจาก 1c ให้นับรวมทั้งใต้ "ฝ่ายคลัง" และ "ฝ่ายออฟไลน์" (นับซ้ำได้ทั้ง 2 ฝ่าย เพราะเป็น
  งานที่ทั้ง 2 ฝ่ายมีส่วนรับผิดชอบคนละขั้น — **ต้องคุยกับผู้ใช้ว่าจะนับซ้ำทั้ง 2 ฝ่ายเลย หรือแยกนับเป็น
  งานของฝ่ายที่ยังไม่เสร็จ ณ ขั้นนั้นๆ เท่านั้น** เช่น ถ้ายังไม่รับคืน = งานของฝ่ายที่ดูแลรับคืน ถ้ารับคืน
  แล้วแต่ยังไม่เก็บ = งานของฝ่ายคลัง)
